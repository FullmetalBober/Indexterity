apiVersion: v2
name: indexterity
description: Indexterity — automatic index management for MongoDB, PostgreSQL and SQL Server (api, dashboard)
type: application
# Chart version — bump on chart changes. `version:set` writes both of these
# line-wise rather than through a YAML parser, which round-trips the document and
# drops every comment in this file along with appVersion's quotes.
version: 0.14.2
# Application version — the default image tag.
appVersion: "0.14.2"
home: https://github.com/FullmetalBober/Indexterity
sources:
  - https://github.com/FullmetalBober/Indexterity
keywords:
  - mongodb
  - indexes
  - database
  - optimization
maintainers:
  - name: Indexterity
