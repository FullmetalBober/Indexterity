{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "indexterity.serviceAccountName" . }}
  labels:
    {{- include "indexterity.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# Indexterity never calls the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
