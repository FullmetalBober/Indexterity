# Decision log

Every load-bearing choice, why it was made, and whether it is still open. Kept
**in the repo** rather than in the wiki on purpose: a decision is worth reviewing
in the same pull request as the change that makes it, and worth being able to
`git blame`.

The prose it used to live at the end of is in the wiki:
[Architecture](https://github.com/FullmetalBober/Indexterity/wiki/Architecture).

Amend an existing entry and set its status to `Revised` (with the month) rather
than appending a second entry about the same thing.

| # | Decision | Rationale | Status |
|---|----------|-----------|--------|
| D1 | Control-plane / data-plane split | Keep prod creds and DB access isolated from the SaaS; enables agent mode later | Locked |
| D2 | Hosted-direct connection for v1 | Fastest onboarding / self-serve; agent is an additive impl, not a rewrite | Locked |
| D3 | Index-only MongoDB role | No data-row exposure in cleanup path; strong trust story | Locked |
| D4 | Hide → observe → drop pipeline | Makes drops reversible until the final confirmed step | Locked |
| D5 | Observe window 30d, adaptive + pattern-aware + configurable | Catches monthly jobs; waiting only delays benefit, dropping early causes outages | Locked |
| D6 | Usage classified by time series (incl. `PERIODIC_DEAD`) | Distinguishes live periodic jobs from decommissioned ones — safer and faster | Locked |
| D7 | "Instant apply" limited to creates, opt-in, size-gated | Auto-create is additive-ish; auto-drop never instant | Locked |
| D8 | App-level envelope encryption via swappable `KeyProvider`, `@noble/ciphers` | Zero cost, no extra infra, no lock-in; KMS/Vault later without call-site changes | Locked |
| D9 | **oRPC** for contracts (was ts-rest) | Migrated Jul 2026 at the founder's call: same contract-first shape (`@Implement` mirrors `@TsRestHandler`), zod 4 native, OpenAPI-standard paths kept so the integration suite survived unchanged (11/12 on first run). Handler errors are ORPCError-coded; the Nest filter now covers only non-oRPC paths | Revised |
| D10 | graphile-worker for jobs | Postgres-backed, no Redis; fits compose | Locked |
| D11 | Demo/read-only default per cluster | Read-only is structural, not a toggle | Locked |
| D12 | npm workspaces (not pnpm) for now | Local Node is Zed-managed; a global pnpm install conflicted. Turbo supports npm workspaces; swappable later | Locked |
| D13 | TypeScript **6** (last JS line); api built with **swc** directly (no Nest CLI) | Was TS 7 (native), but 7 ships no `tsserver.js` until 7.1 — editors broke. TS 6 keeps full toolchain parity (tsserver, programmatic API); typecheck speed is a non-issue at this repo size. Revisit 7 at 7.1 | Revised |
| D14 | **zod 4** (pin lifted) | Landed with the oRPC migration (D9). The api's internal driver-boundary schemas were already v4-compatible (two-arg `z.record`, no deprecated APIs) — zero code changes beyond `z.uuid()`/`z.email()` in the contracts | Revised |
| D15 | **Provisioned least-privilege onboarding** instead of an Atlas API integration | Jul 2026. An admin string is used once to create the `indexterityEngine` role + an `idx_<hex>` user on the customer's cluster; only the scoped string is stored (the admin one never persists). Turns "we can't read your documents" from a promise into a server-enforced guarantee (§10.1), works on any self-hosted/community deployment, and degrades to a guided 422 on Atlas (which owns its user management). Live-verified under `--auth`: full engine surface allowed, find/insert/drop/escalation denied, collect e2e as the scoped user | Locked |
| D19 | **"Cannot tell" is never spelled "all clear"** | Jul 2026. Losing a cluster for days or weeks used to end badly: `$collStats`/`$indexStats` counters are cumulative since mongod started, so a restart during the observe window made `current − baseline` negative, which failed the minimum-ops check and read as *no regression* — the drop then proceeded on evidence that no longer existed, with the pre-flight's `$indexStats` check equally reset. The gate now returns REGRESSED / STABLE / **UNOBSERVABLE**; an unobservable window un-hides and re-proposes (which also ends the case of an index left hidden through an outage), and the create-side watch re-baselines instead of graduating unchecked. Separately, usage findings now require a continuous, current history (≥ 3 snapshots, no hole over 48h) — during a gap a busy index is indistinguishable from a dead one — and repeat failure alerts are capped at one per cluster+task per day | Locked |
| D23 | **The observe window scales to the index's age, not just its usage** | Jul 2026. The window already stretched for periodic usage and shrank for long-proven idleness, but both keyed on the history's *span*, which conflated two different things: how long we have watched, and how long the index has existed. A hand-made ad-hoc index — created, used once by whoever made it, forgotten — has a short span, so it got the full flat month before removal, the slowest possible treatment for the clearest possible case. Two rules added: an index that appeared while we were watching and has never been used is observed roughly as long as it has existed (≥ 7d, ≤ policy), since its entire life is on record and there is no hidden history to wait out; and an index in place ≥ 2× the policy window that saw real use gets 1.5× the policy, because whatever wanted it may want it again on a cadence longer than anything recorded. Age is only claimed when the index first appears at least a day after the cluster's earliest snapshot — collection starts at onboarding, so otherwise every index on a new cluster would look newborn and get fast-tracked, which is the warmup hazard rather than a fix for it | Locked |
| D24 | Api served under `/api`, on the dashboard's origin | The cookie is the whole argument: two origins means CORS plus `SameSite=None`, which by definition attaches the session cookie to cross-site requests. One host with `/api` and `/` path rules makes it first-party instead. Required `setGlobalPrefix("api")` first — better-auth sits on Fastify at `/api/auth`, controllers sat at bare paths, and no single proxy rule covered both | Locked |
| D25 | Server state through TanStack Query, **one key per api call** | Started as three keys — pipeline, telemetry, policy — rather than one blob, so approving a recommendation stops refetching the latency series. Loaders write through the router's query client instead of returning data to re-seed: one cache entry, not two that drift. Extended Aug 2026 to the whole app: a fourth key, `shell`, and every mutation a `useMutation` with a targeted invalidation, so `router.invalidate()` exists nowhere in `apps/web/src`. Three corrections came out of finishing it, all in §14.2. The cache was never reaching the browser — the router serializes loader *return values*, and a loader that fills the cache returns nothing, so every read the server did was done again on hydration (`@tanstack/react-router-ssr-query` fixes it). `ensureQueryData` does **not** refetch stale data, only absent data, which made the "api is unreachable" Retry button measurably inert and means a session change has to *remove* the previous session's entries rather than mark them stale. And a key resolved from "the first cluster" meant two different clusters at two different times, which only a zero `staleTime` was hiding **Revised Aug 2026: the keys are now one per endpoint, not one per reason-to-change.** Grouping by what *moves* a key was a claim about the writes imposed on the reads, and the reads paid for it: the org page fetched a cluster list it never draws, the latency chart could not fail without blanking the collection table beside it, and flipping a cluster to live refetched the member list. Nine `queryOptions` factories replace three `Promise.all` blobs — the request count is unchanged, since a blob of three was always three requests; what changes is that three answers stop sharing one cache entry. Failures stop being caught into an empty payload and become query errors, so `isError` exists for anything that wants to say more than "empty", and each read site defaults to a stable empty value so a dead read still costs one panel rather than the page. `authed`/`apiDown` was the one thing the blob was genuinely computing — it is derived over the three org-level queries now (401 on any of them wins over a transport failure), because no endpoint answers it. The invalidations got *more* precise as a result, and one of them got less: `plan.clustersUsed` lives in the org payload, so creating or deleting a cluster moves the org key as well as the cluster list — the coarse `shell` key had been covering that coupling by accident, and splitting it surfaced a stale "0 / 1 clusters" that the e2e suite caught. Made explicit, and tested. | Revised |
| D26 | `minCount` is a floor **and** a rate | Three sightings meant two different things: `$queryStats` accumulates for the life of the store, the profiler is a ring a busy collection fills in minutes. Both windows turn out to be measurable — `firstSeenTimestamp` and the oldest `ts` — so both are measured. Fortnightly admits a weekly report and rejects a handful of runs since March | Locked |
| D22 | **One auto-approval control, not two** | Jul 2026. `autoApply` (boolean) and `autoApplyScore` (threshold) read like a switch and its dial but were mutually exclusive branches, and the boolean won: setting both silently discarded the number the owner had typed directly beneath the checkbox. The boolean also promoted `ADVISORY_REVIEW` rows, which the score path explicitly excluded — and an approved advisory is worse than useless, because `classify` only deletes and re-inserts PROPOSED rows, so it leaves the refresh pool and is never re-evaluated even after the index starts being used again. `autoApply` is deleted: `autoApplyScore` alone means null = a human approves everything, 0 = everything auto-approves, 1-100 = a confidence floor, advisories never at any setting. Strictly more expressive than the pair, and both bugs stop existing rather than being fixed. Migration carries `auto_apply = true` across as threshold 0 — the behaviour that was actually running | Locked |
| D21 | **The change window picks itself when unset** | Jul 2026. An unset window used to mean "run elective changes at any hour", which is the worst default available: the one moment a drop's brief collection lock is least welcome is peak traffic, and the owners least likely to configure a window are the ones least able to absorb that. The engine now derives one from the cluster's own `latency_samples` — cumulative counters differenced, bucketed into the four 6h slots of the UTC day, quietest slot wins — and re-derives it after every collect so it tracks a workload that moves. Six hours is the honest resolution: collect runs every 6h, so claiming an hour-level window would be precision the evidence does not have. It refuses to guess rather than guessing badly: three clean observations per bucket minimum, the quiet slot must be ≤ 75% of peak (a flat day yields nothing), and intervals crossing a counter reset or a collection gap are discarded. Stored in `inferred_window_*`, apart from the owner's columns, so an explicit setting always wins and clearing it returns to auto instead of freezing the last guess | Locked |
| D20 | **A warning is a defect** (§16) | Jul 2026. Adopted as a repo rule, then applied. Three real faults were hiding behind "just warnings": every oRPC route logged `FST_ERR_REP_ALREADY_SENT` because `@orpc/nest`'s interceptor sends the Fastify reply itself while Nest — which only stands down when a handler declares `@Res()` — sent a second empty one (fixed by wrapping `@Implement` in `src/orpc/implement.ts`, so no route can forget); an unreachable cluster threw five stack traces per task per tick instead of being classified and skipped; and the dashboard rendered `toLocaleString()` during SSR, guaranteeing a hydration mismatch for every reader outside UTC. Each was a genuine behavior bug whose only symptom was log noise **Revised Aug 2026 (#17): a warning only a human can see is not a rule.** The canonical-Tailwind-class warning — `w-[220px]` is `w-55`, the v4 spacing scale being 0.25rem — came from the editor's Tailwind integration and nowhere else. Biome has no rule for it (`useSortedClasses` is about class *order*, and is nursery/info), and Tailwind v4 ships no lint binary, so this one was observable while typing and unenforceable in CI. `scripts/lint-tailwind.ts` now runs inside `npm run lint`. Deliberately narrow: the spacing scale, pixel widths and the fraction percentages, where a value maps to one utility and only one. Nothing about colours or the named scales (`rounded-sm`, `text-lg`), where being sure needs Tailwind's resolved theme — a check that guesses is worse than none, because the first false positive teaches people to skip it. Vendored `components/ui` is exempt, which is #9's decision carried forward rather than a new one: rewriting registry code forks it and the next `shadcn add` puts it back, so the exemption is recorded in the checker instead of left as a standing backlog of warnings. The script verifies its own two dozen cases on every run, because the way a lint script actually fails is by matching nothing and passing forever — verified from both ends, by planting a violation in our code (found, with the right line and suggestion), one in vendored code (ignored), and by breaking the divisor (self-check exits 2). | Revised |
| D18 | **Deny-by-default network guard + invite-only sign-up** (§10.2) | Jul 2026. Onboarding dials whatever an owner pastes, which made the api a request-forgery primitive: with open sign-up, anyone could register and use it to map our internal network, or connect an unauthenticated internal database outright. Targets are now resolved (SRV expanded, IPv4-mapped IPv6 unwrapped, every host in a multi-host string checked) and classified — link-local/metadata forbidden outright, private ranges only with `ALLOW_PRIVATE_CLUSTER_TARGETS`; sign-up defaults to invite-only with first-user bootstrap; a per-user dial budget is consumed before the address check. Self-hosted installs flip both knobs, and the chart warns when the combination is unsafe | Locked |
| D16 | **Engine ports** extracted for future PostgreSQL/SQL Server support (§9) | Jul 2026. `IndexCollector`/`IndexExecutor`/`EngineSession` moved to `src/engine/ports.ts`; adapters register per `clusters.engine` (enum ready, MONGODB the only implementation); the pool, jobs, and API speak only the ports. Capability flags (`hideIndexes`, `provisionScopedUsers`) mark where engines genuinely differ — notably PostgreSQL has no reversible hide, so its adapter will need an alternative observe stage. Behavior-preserving: the untouched integration suite (25/25) passed on the refactor | Locked |
| D17 | **Dynamic observe window** + recurrence floor | Jul 2026. The observe window is decided per drop at hide time from the index's own usage history (`analysis/observe.ts`, stored in `recommendations.observe_days`, reason in the audit trail): periodic usage extends to 2× the largest activity gap (≤ 90d) so a monthly job gets a full cycle inside the window; zero usage across ≥ 2× the baseline shortens to half (≥ 7d). Policy stays the baseline and the fallback. Workload shapes now need ≥ 3 sightings to propose and ≥ 5 for instant apply — a manually-run heavy query once or twice produces nothing | Locked |
| D27 | Internal packages compile to CJS `dist`; apps consume built output | Predictable dev/prod module resolution; Turbo orders builds via `^build` | Locked |
| D34 | **TanStack Charts (pre-1.0) behind a props-stable wrapper; recharts deleted** | Aug 2026. The founder's call on #15, over shadcn Charts — which was the safe answer: recharts underneath, stable, already themed, already installed. Two things decided it. **Size**: removing recharts took the client bundle from 360,985 to 294,441 gzipped bytes, **65 KiB / 18%**, measured by building both branches rather than trusting the library's own consumer-size figure. **SSR**: recharts 3 draws client-side only, so the panels shipped as reserved empty boxes and filled in after hydration; a TanStack Charts scene is SVG computed from a known size, so the server draws the lines (covered by a `renderToString` test, because losing it would be a silent regression to a chart that pops in). The cost is knowingly accepted API churn — 0.6.5 at adoption, and the issue was written against 0.3.1, which is the churn rate stated as a fact rather than a risk. It is contained the way the issue asked: `LineChart` keeps its `{title, unit, series}` props and nothing about marks, channels or scales escapes `components/latency-chart.tsx`, so a breaking release is one file and the callers in `app.index.tsx` cannot notice. Two incidental deletions fell out of it: the recharts wide-row pivot (a grammar-of-graphics mark takes tidy rows, one per series per timestamp) and the label slugging that existed only because recharts read a `db.coll` dataKey as a nested path. `y` accepts null natively, so a gap is still a gap and not a zero. `components/ui/chart.tsx` — the shadcn wrapper around `ResponsiveContainer` — went with recharts; a future `shadcn add chart` would bring it back, which is worth knowing before running one | Locked |
| D36 | **A trust threshold is a duration, never a count of collect intervals** | Aug 2026 (#19). The engine's usage gates were written as interval counts — `minActiveIntervals: 12`, `recentWindow: 3` — and they only meant what they appeared to mean because a collect interval happened to be six hours. Twelve intervals *was* three days of traffic and three trailing snapshots *was* the last twelve hours, at that cadence and no other. Change `0 */6 * * *` to `*/15` and the same numbers silently buy an eighth of the evidence: the engine starts calling an index dead on three hours, with no code change, no config change, and nothing failing. That is the trap, and it is the reason this landed **before** any cadence change rather than with one. `minActiveIntervals` becomes `minActiveHours: 72` and `activeIntervals()` becomes `activeHours()`; `recentWindow` becomes `recentHours: 12`. The second was not in the issue and is the more dangerous of the two: it is the line between PERIODIC_ALIVE and the **droppable** PERIODIC_DEAD, so at a fifteen-minute cadence a nightly job that had simply not run yet today would have read as decommissioned. `minHistory` deliberately stays a count — it is how many data points distinguish a pattern from a flat line, a question about samples rather than time, and `minHistoryDays` beside it is the durational half of the same gate. Two things fell out of writing it. `activeHours` credits each interval at most the series' **median gap**, taken from the data because a pure engine cannot know what the scheduler is set to: without the cap a single outage would credit its whole length as traffic the moment the counter had moved anywhere inside it, so one dark afternoon could manufacture the three days a drop needs. And the tests had to grow timestamps — a fixture where every snapshot shared one `capturedAt` could express "three snapshots" but not "went quiet", which is exactly the confusion being removed. Separately, and the part the issue identified as the real complaint: **a newly connected cluster is collected immediately**, queued from `storeCluster` and best-effort, so a failed enqueue cannot turn a connect that already committed into an error. Waiting up to six hours to see anything was what read as "the cadence is too long", and one job on connect answers it without changing steady-state load at all. The cadence itself is left at 6h on purpose — it is now *safe* to change, since none of the numbers move with it, and what remains is a cost question (how much collection load buys how much reduction in the one-interval blast radius of a mongod restart) that deserves its own decision rather than a ride on this one | Locked |
| D37 | **Tenancy is better-auth's organization plugin; entitlements are not** | Aug 2026 (#71). `organizations`/`members`/`invites` and nine oRPC routes were ours, and the plugin ships all of it plus the two verbs the product was missing: you could not create an org, and you could not delete one. Orgs appeared as a side effect — `resolveMembership` inserted "My Org" behind the first authenticated request, and `acceptOrgInvite` deleted that shell again if it turned out to be empty. Seven mutations moved to `/api/auth/organization/*`, including the last-owner rules we had implemented twice, in two places, with two different error messages. Three things were deliberately kept. **The keys stay `uuid`**: the plugin's models are mapped onto the existing tables and column names (`modelName`, `fields`) and handed a `crypto.randomUUID()` generator, because three tables carry a cascading `org_id` and the contracts type it `z.uuid()` — retyping that after subscriptions exist is exactly the migration the billing comment warns about, and doing it now costs a config block instead. **`maxClusters` is not a plugin concept** and `billing/plans.ts` does not move; the plan columns are `additionalFields` with `input: false`, so they are readable through the plugin and settable by nobody through it. **One seat limit, not two** — `membershipLimit` counts members, ours counts members plus outstanding invites, so the plugin's is set to Infinity and the rule is enforced in its invite/accept hooks against the same table everything else reads. `organizationLimit` is likewise unset: its function form returns a *boolean*, and its refusal is a bare 403 naming neither the limit nor the remedy, where every plan refusal here is a 402 that does both. An `organizationLimit` was briefly added on the reasoning that unlimited free orgs is a free product, and **removed the same day**: a plan is bought per org, so a cap on how many you may make is a cap on how much you may buy, and it also gave a paying customer a quota with no way to spend it. The free tier is held by the cluster limit instead, applied inside each org one at a time. Two behaviour changes a reader will notice: the switcher is per SESSION now (`session.active_organization_id`), so two browsers can sit in two different orgs; and an invitation is addressed rather than bearer, so any invite-link format we published stops working | Locked |
| D35 | **The unbounded tables virtualize with spacer rows, not absolute positioning** | Aug 2026. Recommendations is collections × indexes and the per-collection footprint is one row per collection; neither `listRecommendations` nor `getCollections` applies a limit, so both are unbounded in the only direction that matters — a large customer. Virtualized with TanStack Virtual behind one `virtualize` prop on `DataTable`, and three things about *how* are the decision. **Spacer rows rather than absolutely-positioned ones**, which is what the library's own table examples do: a `<tr>` taken out of the flow stops sharing the table's column widths, so the columns lose alignment the moment you scroll, and it stops being a row to a screen reader, which is most of what a `<table>` is for. Two `aria-hidden` spacers of the right height buy the same DOM saving and cost the table nothing — and because `getAllByRole` skips aria-hidden nodes, the existing order and filter tests kept working unchanged, which is the same fact from the test side. **The height is a maximum, not a height**, so a cluster with four collections renders at its natural height with no scrollbar and looks exactly as it did; only the long tail caps and scrolls, and only the long tail was the problem. **`initialRect` is load-bearing**: the server has no element to measure, and without it the virtualizer reports a viewport of zero and renders no rows, which is how a virtualized table silently stops being server-rendered — the failure mode the TanStack DB spike had just demonstrated. Proven by a `renderToString` test asserting the first row is in the HTML and the last is not, because the e2e SSR test cannot see it (the e2e cluster has no collected rows). Two things this does **not** fix, deliberately: the payload is still unbounded, since virtualizing is a render-cost fix and a cluster with 20k recommendations still ships 20k rows of JSON — that wants a limit or a cursor on the contract, which no read currently takes; and the audit trail is left alone because the api already caps it at fifty. One test-harness consequence: jsdom performs no layout and reports `offsetHeight`/`offsetWidth` as 0, which are the exact two properties the virtualizer measures, so `vitest.setup.ts` now shims them at a plausible scale (600px container, 40px rows) — left at zero every table test fails on markup that is correct in a browser | Locked |
| D33 | **Tables are headless; the sort order is a decision, not the api's leftovers** | Aug 2026. The recommendations, per-collection and audit tables were `<table>` markup with the rows mapped inline: no sorting, no filtering, a header row kept in step with the body's by hand, and a 379-line route that was mostly cells. Fifty recommendations arrived in whatever order the api returned them, which is the one order nobody wants. TanStack Table for the logic, the **same shadcn primitives** for the markup, one `DataTable` for all three — so this is not a visual rewrite, and the column defs carry what the markup could not say: how to read a value, how to draw it, and how to sort it, in one object. **v9, not the v8 that shadcn's data-table page documents**: 9.0.0 is the `latest` tag, the repo's standing rule is latest stable (§2.6), and v9's opt-in feature registry is a real gain here — what a table can do is settled statically, so the grouping, pagination and row selection none of these three use are not in the bundle. The cost is that it had to be written from the type definitions rather than from any published example, and the repo's vendored skill still documents v8. Three things the conversion decided rather than inherited. **A default sort per table**, chosen from what the table is for — recommendations by score descending, collections by index footprint descending, the trail newest-first — and sorting cannot cycle back to *unsorted*, because that state is the api's arbitrary order and no reader asks for it on purpose. **A missing number is not zero**: an unmeasured collection sorts as `-1` rather than ranking beside a genuinely idle one. **Empty is two different facts** — nothing collected yet gets a shadcn `Empty` explaining what will appear and when, an over-narrow filter gets a row saying so and quoting what was typed; the old markup drew a bare header over both. Filtering is global per table rather than per column, which is what the actual questions want ("what happened to this index", "which of these failed") and is one input instead of eight | Locked |
| D32 | **Forms validate against the api's own input schemas** | Aug 2026. Seven `useState` calls held one sign-in form, and the shape repeated in the connect-cluster, policy, team and reset-password forms; the `busy` flag alone had to be cleared on all four exits from a submit handler, including the early returns. TanStack Form replaces the state, but the reason to do it now was the validation, which did not exist: the client submitted whatever was typed and relayed the api's 400. The rules were only ever written on the api, so the fix was to name them — the inline `z.object` literals in `contract.ts` moved to `contracts/src/inputs.ts`, `contract.ts` composes them into route inputs, and each form field validates against the same object (`signInInput.shape.password`, `policyKnobsInput.shape.observeWindowDays`). Not a second copy of the rules, the same copy: a message added for a reader also improves the api's 400. better-auth is included by inversion — `minPasswordLength` is now *set* from `PASSWORD_MIN_LENGTH` rather than left to the library default, because a default that happens to match is not a shared rule. Three things fell out of writing it. **Mutation hooks take their values in `mutate()`, not as hook arguments** — they took them at render because the values lived in `useState` and were current on every render; a form store deliberately does *not* re-render on a keystroke, so a closure captured at render submits the second-to-last thing typed. **The submit button is never disabled for being invalid**, which is the obvious wiring and wrong: `handleSubmit` refuses an invalid form anyway *and* touches every field on the way, which is what reveals the errors — gate the button and the change window's paired hours reach a state where the button is grey, nothing is highlighted, and no click explains why (found by a test, not by review). **A half-set change window is now refused rather than silently completed**: the old code nulled whichever hour had no partner, so a number the owner had just typed vanished with no reason given | Locked |
| D31 | **The browser calls the api directly; the web server is not a BFF** (§14.5) | Aug 2026. 28 `createServerFn` wrappers existed for one reason — two origins, so a direct call would need CORS with credentials and a `SameSite=None` session cookie. #28 put the api under `/api` on the dashboard's host, which removes the reason, so the wrappers, `decodeOnce` (#16, moot) and one hop per call all go. The alternative was keeping the relay as a documented fallback for split-origin installs, and that was **rejected**: supporting both means maintaining both, and the fallback is the path nothing exercises. Same-origin therefore has to hold, and it is the APP that guarantees it: the dashboard server answers `/api` by forwarding it when nothing in front does (§14.5), so a split-port install still works and a proxy rule becomes a way to remove the hop rather than a prerequisite. The first cut made it a deployment requirement instead, with the rule repeated in four places — ingress, an nginx service in compose, the vite dev proxy, a node proxy for the e2e suite; moving it into the app deleted three of them, leaving only the ingress, where it is now an optimisation. Genuinely split origins were rejected separately — that costs `SameSite=None`, CORS with credentials, and an origin check the oRPC pipeline has never needed. The cost accepted is that the api is publicly reachable, so its rate limiting, origin checks and auth guard are the only line of defence rather than the second (#21 in the same breath). It also unblocks #22 — SSE from the browser straight to the api is materially simpler than streaming through a relay | Locked |
| D30 | **The chart ships the alerts, not just the metrics** (§15.2) | Aug 2026. Three scrape endpoints and a blank Prometheus is not observability — it leaves every operator to derive the same queries from metric names they have to discover first, and two of those queries are ones a careful person still gets wrong. `increase()` cannot see a dead process: a stopped worker's series go stale, and a stale series matches no `== 0` comparison, so the intuitive "no successes recently" rule is silent in the one case that matters (`absent_over_time` on a gauge only that process reports is what fires). And the schedule lives in the `schedule*` dispatchers, not the per-cluster tasks — `collect` legitimately stops when the last cluster is offboarded, so a rule watching it pages on a successful offboarding. Also folded in: the api's health endpoint answers `ok` without touching Postgres, so a lost database is invisible to both probes and needs an alert of its own. Rules are validated in CI with `promtool`, because a typo'd expression passes `helm lint` and `kubeconform`, installs cleanly, and then never fires | Locked |
| D29 | **The dashboard server reports three things, and per-function metrics are not one of them** (§15.1) | Aug 2026. D28 covered the api and the worker, which left the layer the reader waits on unmeasured. Most of what the dashboard does is already visible from the api's side — every server function call lands there — so the question was which of the remainder is worth a seam. Three are: render time per route pattern, a response the api never heard about (a loader that threw, a 404), and the api measured from this end of the network, which is the only place a *partial* api failure is recorded at all, because the loaders catch everything and render `EMPTY_PIPELINE` rather than an error. One seam serves all three: `src/server.ts`, SSR-only by construction, which also keeps the SDK out of the browser bundle. Per-function metrics were built, worked, and were **removed**: a server function here is one to three api calls and almost no work of its own, so its duration is those calls plus noise, and naming it cost a second framework seam (global *function* middleware — request middleware cannot see a direct SSR invocation — plus an SSR-guarded import in `src/start.ts`). The instrument that restates another one is the one to cut. Extracting `packages/metrics` came first regardless, so the exporter stays one decision rather than three | Locked |
| D38 | **Live dashboard = SSE + invalidation; postgres NOTIFY is the transport** (#22) | Aug 2026. A worker pass used to be invisible until a reload or an unrelated mutation refetched the loader. Three choices, each the cheap end of a real alternative. **The event is a signal, not state**: `{kind, task}`, no rows — the client answers with `invalidateQueries` on the keys that pass moves (a pure, tested mapping beside the mutations' own named invalidations, D25), so the dashboard's one copy of server state stays its only copy. TanStack DB's sync-source pitch was re-examined and re-declined on the #42 grounds — reacting to an event is one line, and `useLiveQuery` still cannot SSR. **The route is a contract procedure**, not a Nest `@Sse` bolt-on: oRPC's `eventIterator` streams through `@orpc/nest` on Fastify (verified in the installed dists before writing any code, per the founder's tRPC question — switching RPC layers for subscriptions was already answered "no" in the nestjs-trpc note), so the browser subscribes typed, first-party-cookied, through OpenAPILink like every other read. Ownership is checked once at connect; the api ends every stream at five minutes so the reconnect re-authorizes on the same clock as the session cookie cache, and NOT_FOUND — not an empty stream — answers a cluster that is not yours, because an empty stream is a hang, not an answer. **Worker → api is postgres NOTIFY** because the worker is a separate process on purpose and an EventEmitter would emit into the wrong one — the transport both processes already share (D10), the mechanism graphile-worker itself uses, and the api already writes the worker's queue in raw SQL. Every replica LISTENs on a dedicated client (LISTEN binds to a session, so never a pooled one) and fans out to its own subscribers; embedded-worker installs traverse the same path. Emissions are best-effort and swallow their own failures: a pass that landed must not retry — and re-run `executor.hide` — because a nudge could not be sent; a lost event costs staleTime of freshness, never correctness. Two operational edges came out in review rather than production: the SSE route's response duration is excluded from the latency histogram (a stream's elapsedTime is its lifetime; five healthy minutes would own the p99 and page someone), and open streams are aborted in `onModuleDestroy` — before Fastify's close, which waits on active requests — so an open dashboard cannot hold SIGTERM until the SIGKILL | Locked |
| D28 | **Metrics on a second port, instrumented through OpenTelemetry** (§15.1) | Aug 2026. There were none: a service that hides and drops indexes on other people's production clusters could not state how many clusters it currently cannot reach, how long its queue is, how many drops are mid-observe, how often the regression gate fires, or what its dead-letter rate is. OpenTelemetry rather than a Prometheus client library so the exporter is a decision in one file — the chart ships a ServiceMonitor today, and an install that wants an OTLP collector should not need a rewrite. A second port rather than a route under `/api` because the endpoint has no auth and the ingress publishes the api host. Two things fell out of writing it: queue depth belongs in SQL over `graphile_worker.jobs`, not in worker-held counters that vanish on restart and report zero when the worker is *gone*; and the unreachable count has no source other than the worker's own memory, because an unreachable cluster is a handled condition (§7.4.1) that the queue records as a success | Locked |
| D39 | **The collect cadence is hourly, and `latency_samples` is the ceiling on it** | **Revised Aug 2026** (#178) — half of the reason given below for not going shorter has since been removed, and hourly stays anyway. `getLatencySeries` was bounded on both axes by D47, so the payload argument is settled; what survives is that D47 capped the *response*, not the *query* — `loadLatencyReadings` selects every collection's rows inside the window and takes the top 8 in JS afterwards, so a dashboard load still scans collections × cadence × up to 30 days of the one table that never run-length-collapses. Against doubling that scan and doubling the storage held inside `RETENTION_DAYS`, half-hourly buys 48 points a day instead of 24, and the floor this cadence was raised to clear was four a day — hourly clears it six times over, and the signal that has to be fast is the five-minute read-pressure probe's, not this one's. So the cadence is unchanged and the reasoning under it now rests on what is true; the answer would change if latency history were downsampled and that read sliced in SQL. Original: Aug 2026. D36 deliberately left this open — it made the cadence *safe* to change by expressing every trust threshold in hours, then declined to change it, on the grounds that what remained was a cost question deserving its own decision. This is that decision. The cost is not storage: run-length rows made index history nearly free, and 76% of looks on real data collapse into an extended run rather than inserting one. It is **one table**. `latency_samples` collapses none of them, because `$collStats` totals move on any operation at all — a live collection differs at every look, so there is nothing to run-length, and that table alone grows in step with the cadence while index history grows at about a quarter of the rate. Hourly buys a usable latency trend where 6h gave four points a day, which is the reader-facing reason; the engine-facing one is that a mongod restart's blast radius on the observe window is one interval wide. Going *shorter* than hourly is deliberately not taken here: it wants `getLatencySeries` bounded and old latency history rolled up first, and neither exists. Load was never the binding constraint — a collect runs under a second against a hundred collections, and about a round trip per collection against a remote one | Locked |
| D41 | **The auth rate limit is counted in Postgres; the Fastify one stays per replica** (#54) | Aug 2026. Two limiters ran in front of this api and neither was the one it claimed to be. `AUTH_RATE_LIMIT_MAX=20` was documented and advertised, while the limit that actually governed a sign-in was better-auth's own undocumented default of 3 per 10s — measured, 22 of 25 attempts throttled under `NODE_ENV=production` and 0 with it unset. And both kept their counters in process memory, so with `api.replicas: 2` the real ceiling was double the configured one, a rollout handed every bucket back at zero, and the limit was **non-deterministic under load**: which pod a request landed on decided which bucket it spent, so an attacker spreading requests got the sum while a user behind a shared NAT could trip on one pod with another sitting empty. better-auth's half now counts in `rate_limit` in Postgres (it prunes the table itself) and its `customRules` price the credential endpoints at the configured number per minute, so the knob moves the limit that applies. Two defaults are deliberately left alone: the GLOBAL auth limit, because the dashboard reads its session through `/api/auth` on ordinary navigation and pricing that at the credential number throttles reading rather than guessing; and the mail-sending endpoints, rationed at 3 a minute because each sends mail to somebody who did not ask for it. Redis for both limiters (the issue's option 3) is correct and declined: it would be the first hard dependency this control plane takes on something other than Postgres, and the Fastify limiter's per-replica behaviour is documented instead of implied. Both limiters are kept — better-auth's never sees the oRPC routes and is off outside production, where Fastify's is the only thing in front of `/api/auth`. One thing writing it turned up: better-auth resolves a client address only from a forwarded header it can attribute, and with no `trustedProxies` list it believes `X-Forwarded-For` **only when it carries one address** — which it never does once an ingress has added itself, so every caller was landing in a single shared bucket, a limit that throttles your users and not an attacker. `TRUST_PROXY`'s CIDR entries are handed to it as that list, and the docs now say to prefer a list over `true` | Locked |
| D42 | **A second audit table for owner-level acts, and the actor comes from the request** (#53) | Aug 2026. `actions` is per cluster, hangs off a recommendation and ages out with the plan's history window; it can say a drop was approved by user X and nothing about how X became an owner. So `security_events` is a **separate** table, per org, with a nullable cluster: authentication, membership and a cluster's access, one row per act, never updated. **Nothing cascades into it** — every foreign key is `set null`, and the actor's email and the target's name are stored beside the ids — because a trail that a deleted org takes with it answers nothing about the org that was deleted. **It does not age out** on the plan's clock: the incident that needs these rows is usually older than the day it is noticed. **IP and user agent are stored**, no more personal data than `session` already holds, because "an owner, from somewhere, on something" is not an answer; the address is null rather than wrong when `TRUST_PROXY` says no proxy is in front. The actor is resolved from the REQUEST, not from a database hook: `databaseHooks.session.create` sees a sign-in and the organization plugin's `afterUpdateMemberRole` sees a promotion, but the plugin is handed the member and the org and never the caller — so an after-middleware with better-auth's own `getSessionFromCtx` is the only shape that can name who did it. Two things that measurement, not reasoning, established: `ctx.context.session` is empty in an after-hook even on routes that required one (every org event landed anonymous until the integration suite asserted otherwise), and a sign-out cannot be recorded there at all because it has already destroyed what would identify the actor — that one comes from `databaseHooks.session.delete`, whose payload IS the session that ended. Recording is best-effort and logged: a table problem must not stop people signing in, and refusing the sign-in would not un-record it either. Costs `previousRole` on a role change, which only the plugin hook is given; the row says what the member is now and who made them that, and the role before is the previous row for that target | Locked |
| D49 | **One environment schema per process, validated at boot — and the schema is what holds the four homes to each other** (#126) | Aug 2026. Nothing validated the environment. Twenty-eight `process.env` reads were scattered across the api, the web server had three more, and the worker inherited whatever the api's modules happened to pull in at import time. The failure modes were not merely inconsistent, they were *deliberately* inconsistent: `requiredEnv` threw at import time in `auth/index.ts` and at request time everywhere else, so which variables a deployment was missing depended on which endpoint someone called first. **One rule replaces all of it: absent is fine, malformed is fatal.** An unset optional knob still takes its default — that half was right, and a schema that demanded every knob would be worse than what it replaced — while a value that is present and wrong is a boot failure naming the variable and what it expected. `positiveEnv("AUTH_RATE_LIMIT_MAX", 20)` read `2O` (letter O) as 20, so a fat-fingered brute-force budget looked correct; `trustProxySetting()` returned `false` for anything it did not recognise, so `ture` and a CIDR list with one bad range both meant "nothing in front" and collapsed every per-client rate limit into one shared bucket (D41's hole, arrived at from the other direction). **Three schemas, and the layering is the deployment's, not a convenience**: `migrate` wants `DATABASE_URL` alone because the Helm pre-install hook gives that Job nothing else; `worker` adds `MASTER_KEY` and the cluster-dialling guards; `api` adds `BETTER_AUTH_SECRET` and everything HTTP, which the chart's worker Deployment sets none of. `MASTER_KEY` on the worker is the one that changed behaviour — a worker booted without it started cleanly and failed at the **first job that opened a cluster**, as a decrypt error hours after the deploy that caused it. **`@nestjs/config`'s `validate` was not enough on its own**, which is why validation lives in a side-effect module the entrypoint imports first (`env.api.ts`, `env.worker.ts`): every import of an entrypoint is evaluated before the first statement of its body, and `auth/index.ts` asks for its values while it is being imported, so a hook that runs at `NestFactory.create` would already be too late for the module that needs it most. A misconfiguration prints its report and exits 1 rather than throwing — the stack of a config error says only that the config was loaded. **`@t3-oss/env-core` for the web, and the split is the whole reason**: `api-origin.ts` and `api-passthrough.ts` read `process.env` in modules that sit in the SSR graph, and nothing structural stopped one of those values being read from a module the client build also pulls in — a runtime address inlined into the bundle is what broke compose in #2. Every variable is `server`, so reading one in the browser throws; a value the browser needs must be declared with the `VITE_` prefix that makes it public by construction, and there are none. That pass found the dashboard reading `TRUST_PROXY === "true"` while the chart hands it the same CIDR list it hands the api, so a configured proxy read as no proxy and the passthrough stripped the very header the api counts its limits by. **Deliberately NOT done: making SMTP mandatory under `NODE_ENV=production`.** The issue raised it, and it is wrong twice over — `NODE_ENV=production` is set by the image rather than by "hosted", so it would break every self-hosted upgrade, and mail-disabled is a supported posture the docs already describe. What was wrong was the *half*-configured case: a host with no credentials built no transport, so the deployment sent nothing while every invitation, reset and emailed two-factor code reported success. That is now a boot failure naming the missing half, and leaving `SMTP_HOST` unset is still how a deployment says it does not send mail. **The schema is the source of truth for the four homes** — `.env.example`, `docker-compose.yml`, the two test harnesses, and the chart — and `config/homes.test.ts` holds all four to it in both directions: a required variable the chart never sets fails the test rather than the deploy, and a variable a home sets that no schema knows is a typo nothing else would have caught (`REQUIRE_OWNER_2FA` misspelt in the chart reads as "off" forever). It found four knobs settable through nothing but `extraEnv` (`RETENTION_DAYS`, `ALLOW_UNTESTED_MONGO_VERSION` and both rate limits), a `WEB_PORT` nothing had read since the dashboard's port became nitro's, and two `VITE_*` build args in the web Dockerfile that outlived #2 — all now closed. Two side effects worth naming: `billing/plans.ts` stopped reading the environment and takes the operator's ceiling as an argument, so the file that claims to be pure finally is; and `DEFAULT_ORG_PLAN` turns out to reach nothing — the organization plugin inserts without a plan and the column defaults to `FREE` — which is left alone here because changing what plan an org lands on is an entitlement decision, not an env-validation one | Locked |
| D50 | **The engine may re-order a protected index's key DIRECTIONS, because that removes nothing — approval-only, hinted indexes vetoed outright** (#127) | Aug 2026. `isNeverDrop` protects `_id_`, unique, TTL and shard-key indexes, and `compatible()` excludes all of them from being extended or merged — correctly, because dropping a unique index removes a constraint no amount of latency measurement makes safe. But a RE-ORDER is not a drop. **A unique index's guarantee is a property of its key SET, not of its key DIRECTIONS**, so `{a:1,b:1}` unique and `{a:1,b:-1}` unique enforce exactly the same rule — verified against mongod 8.2 rather than reasoned about: both coexist, a duplicate is refused while both are present, and after dropping the ascending one the descending one still refuses it. **Its own type rather than riding UPDATE**, which is the question the issue posed. UPDATE means "extend to a wider key set" in the rationale text and on the dashboard, and the type is also what scopes three rules that must not widen: approval-only, the hinted veto, and the one place a protected index may be dropped at all. **Approval-only whatever the threshold says**, enforced in `promoteByScore` beside the advisory exclusion rather than by scoring it low — a threshold set to 0 must not reach it. Not because the guarantee is in doubt but because a change to a constraint-bearing index is a different FELT risk from adding one, and the person who answers for that database is entitled to look at it. **A hinted index is a hard veto.** `.hint("a_1_b_1")` against an index that is now `a_1_b_-1` is an ERROR, not a slower query, and the DEFAULT NAME encodes the directions — so a hint breaks by name as surely as by key pattern, and nothing downstream would catch it: the post-build watch measures WRITE latency, and the broken queries would already have stopped running. `collectHintedIndexes` already existed for the analogous reason on the drop side. **Build before drop, never the reverse**, which the existing create → watch → graduate → retire sequence already does; the requirement here is that this class is never allowed a shortcut through it. Both indexes enforce the constraint while both exist, so there is no instant without it — dropping first would open one, and a duplicate written in that window makes the rebuild fail and leaves the collection with NO constraint. **How the drop is finally permitted, without widening anything.** `isNeverDrop` is untouched and `preflightDrop` still refuses a protected index on sight, UNLESS the row names the index that replaced it — and that claim is then checked against LIVE state at the moment of the drop (same fields in the same order, only directions differing, every option identical, present and not hidden). If the replacement was itself dropped, hidden or rebuilt differently in between, the drop is refused and the original stays. The exemption is tied to a specific replacement rather than to a class of rows, so it cannot leak into the ordinary redundancy path. **Options travel on the row, not from a live read at build time**: `unique`, `partialFilterExpression`, `sparse` and `collation` are recorded when the proposal is made, because by build time the original may already have been changed by somebody else and inheriting that silently is how an option gets dropped. A build with none recorded is refused rather than attempted. **Three things writing it turned up.** The undo path rebuilt a dropped index from `{name, collation}` alone — sound only while never-drop meant a unique index could never be dropped, and a silent constraint removal the moment one could, so it carries every option now, for every undo rather than for this one. A flip that would break a shape the index serves TODAY is refused: nothing measures that afterwards (same reason the hint veto exists), so which pattern to keep is a judgement about the workload and the engine does not make it. And a protected index with mismatched directions used to produce NOTHING AT ALL — `sortOrderAdvisories` excluded it from being a blocker while `recommendCreates` suppresses the create whenever an index covers the fields, with no such exclusion — so the engine had a finding and said nothing about it, forever. The issue described that as "surfaced as ADVISORY_REVIEW and nothing else"; it was worse than that, and advisories cover protected indexes now. **What is NOT claimed here.** The issue asked for a measurement of the addressable set on real collected indexes BEFORE any engine code, and that measurement was not possible — it needs a fleet this branch cannot reach. What is shipped instead is the instrument: the addressable set is exactly what `recommendReorder` emits, so the count is a query away on any deployment, and the exclusions that make it small (single-field, TTL, shard key, `_id_`) are each a fact about MongoDB rather than a policy — measured on 8.2 and stated in `analysis/reorder.ts`. If the count comes back near zero on real fleets, what is here is a narrow feature nobody trips over rather than a wrong one | Locked |
| D51 | **The plan a new organization lands on is stamped by the create hook, and the column lost its DDL default so forgetting is loud** (#132) | Aug 2026. `DEFAULT_ORG_PLAN` was set by the chart, documented in `.env.example`, validated at boot by D49's schema, and read by **no production code** — `defaultOrgPlan()`'s only caller was its own unit test. The organization plugin inserts without a `plan`, so `organizations.plan`'s DDL default decided, and that default was `FREE`. **Every self-hosted install therefore ran on the hosted free tier**: 3 seats, no unattended apply, 90 days of history, while `config.defaultOrgPlan: SELF_HOSTED` reached the container and stopped. The BUSL cluster cap is 1 either way so the licence was never at stake — what was at stake is that `billing/plans.ts` says shipping self-hosters `FREE` "would restrict them further than the licence they are complying with", and that is exactly what happened. **Three choices, and the third is the one worth arguing about.** *Where*: `beforeCreateOrganization` returns `{data: {...incoming, plan}}` rather than an `afterCreateOrganization` UPDATE, so the row is never momentarily on a plan the deployment did not ask for — a worker reading it in between would enforce the wrong limits. It is exported and database-free precisely so a test can call it; a hook reachable only through better-auth is a hook nothing can pin, which is how the bug survived. *Existing orgs*: `set-plan.js --backfill <PLAN>` prints what it would move and changes nothing until `--apply`, guarded three ways — `plan = FREE` (the plan the DDL default imposed, so this can raise a plan and never lower one), `planUpdatedAt IS NULL` ("nobody ever chose this") and `billingSubscriptionId IS NULL` ("no provider owns this") — so it is safe to hand a HOSTED operator the same command. **The first guard was added after running the dry run against a database with history in it**: `planUpdatedAt IS NULL` alone matched organizations sitting on `SCALE`, because anything that writes the column without also writing the timestamp — an integration test, a hand-run UPDATE — is indistinguishable from an org nobody ever chose for, and every one of them would have been silently downgraded by a command called backfill. The plan is an argument and never `DEFAULT_ORG_PLAN`, because a wholesale silent move driven by a variable the operator cannot see is the shape of the bug being fixed. *The DDL default*: **removed.** A default on a column nobody writes is not a fallback, it is the decision, and it made the failure silent for a release. Without it a path that forgets fails at the insert — and because every integration and e2e test creates an organization, a better-auth upgrade that stopped honouring the hook's return value would fail CI instead of quietly re-tiering self-hosters. `planFrom`'s fallback to `FREE` stays, because it is the right reading of an unrecognised *stored* value and was never the thing reading the environment | Locked |
| D48 | **Errors go to Sentry, off unless a DSN is set, and the scrubber is the part that is tested** (#31) | Aug 2026. There was no error reporting at all: a job that threw in the worker was invisible unless somebody happened to be reading container logs at that moment, which for a service that hides and drops indexes in other people's production clusters is the wrong default in the workload least likely to be watched. **Sentry rather than Datadog, and the reason is #20 rather than a feature comparison**: D28 and D30 already put measurement on OpenTelemetry behind a Prometheus exporter and shipped the alert rules that read it, so the one-pane-for-everything pitch would mean retiring what the chart just gained, and Datadog's per-host agent is not something a chart can offer an operator as an optional value — its Database Monitoring, the one feature that sounds decisive for a MongoDB product, watches OUR Postgres and can never be installed in a customer's cluster. **The DSN is the operator's, and absent by default** in compose, in `.env.example` and in the chart: a self-hosted install has no business reporting to us, and no DSN means `Sentry.init` is never called rather than called with an undefined one. **Two projects, and the seam is the deployable rather than the workload.** The api and the worker share one and are told apart by a `service` tag: they are one image, one release and one body of code — `jobs/`, `analysis/` and the drizzle layer are reachable from both — so a fault in them is one issue a per-workload split would file twice, and `RUN_WORKER=true` settles it by making one process both, leaving no DSN such a split could correctly pick. The dashboard gets its own, because it shares nothing with the api but the contract and has its own release. Every process reads a plain `SENTRY_DSN` and the deployment decides which project that is — `SENTRY_DSN_API` / `SENTRY_DSN_WEB` in compose, `errorReporting.dsn` / `errorReporting.webDsn` in the chart, the latter falling back to the former so a small install can keep one. The real DSNs live in the gitignored `.env` and nowhere else: a value committed to `.env.example` or to a chart default would make every fork report to us, which is a data leak with a nice UI. **Reported once, and only for faults.** The dead-letter transition rather than every retry (the counters already say how often jobs retry: metrics say how often, errors say what), and every task rather than only the per-cluster ones, because `retention`, `digest` and the dispatchers have no owner to email and so had no audience at all. **better-auth answers its own failures and so needed a second seam**: its api layer catches everything a route throws, logs `# SERVER_ERROR` and returns a 500 itself, so nothing propagates to the filter and the most security-sensitive surface in the product was reporting nothing — again found by stopping postgres rather than by reading. Reported from the Fastify mount that already inspects the status, which costs the stack; better-auth's `onAPIError.onError` would hand over the original error but is absent from 1.6.24's type declarations, and a cast a minor upgrade can silently invalidate is a worse trade than a status and a route. On the api the rest of this hangs off the last branch of `AppExceptionFilter` BY HAND rather than off the SDK's `@SentryExceptionCaptured()`: that decorator's own filter stands down for `HttpException` and nothing else, so it would report a rate-limited client, an oversized payload, a 404 and an unreachable cluster as unhandled faults — three of which are the service working, and the fourth a handled condition (§7.4.1) that would page us about a customer's firewall. **What is scrubbed is the part with tests.** The MongoDB driver puts the connection string in the MESSAGE of what it throws, where no header rule looks, so `packages/errors` walks the WHOLE event rather than a list of known fields — a field list is correct until the SDK adds a field, and it fails silently — removing database URIs whole (hosts included: a replica set's member list is the customer's infrastructure), the credentials of any other URI, and any key named like a secret, a superset of the Fastify logger's redact list that #31 set as the floor. It is a pure function in its own package for the same reason the analysis engine is: it can be proved without a DSN, a network or an SDK. **And `sendDefaultPii: false` is not what its name promises** — this cost a live test to learn, which is why the wiring was exercised against the running stack rather than signed off from unit tests. With it set, a real 500 on `/api/auth/sign-in/email` (postgres stopped, so better-auth's own handler threw) arrived at a collector carrying `request.data`: the sign-in body, password included. In 10.69 the body, stack locals and database payloads are governed by `dataCollection`, whose defaults collect all three — and each is a route the string scrubber cannot see: `POST /clusters` carries a customer's connection string in its BODY, the locals of a collect frame hold the DECRYPTED string (sealed at rest is no defence in a stack frame), and every job selects `sealed_dek` / `sealed_data` off the clusters table. All are turned off, and the body is ALSO dropped whole in `beforeSend`, because setting `httpBodies: []` did not stop it arriving — measured twice, so the scrub is what holds and the option is kept only because it is the documented control and should one day work. Two smaller findings from the same run: a secret inside a JSON-encoded string is invisible to key matching (`password` in `{"email":…}` is not a key of anything, it is characters in a scalar), and console breadcrumbs carry whatever the process logged, which is why they are scrubbed too. **Two shapes the SDK's own quickstart asked for and did not get.** Tracing, profiling, metrics and session replay are all off — a second timing pipeline answers D28's questions where D30's alerts cannot see them, and replay records a dashboard that shows a provisioned connection string once. And the dashboard initialises inside its module graph instead of through the documented `--import instrument.server.mjs` preload: the web image ships `.output` alone, and what lands in its `node_modules` is what nitro TRACED from the graph, so a preload file copied in beside it is not what causes the SDK to be there — the in-graph import is. The preload's one advantage, patching modules before the first require, buys distributed tracing, which is off | Locked |
| D52 | **`npm audit` is gated on the half of the tree that SHIPS, ZAP runs passive against the dashboard's origin, and every exception is a written reason** (#21) | Aug 2026. There was no security check in CI at all, and `npm audit` reported eleven advisories nobody could act on as a group. **The gate is not `npm audit`**: a bare run counts the dev-server hole in `drizzle-kit`'s bundled esbuild the same as one in the HTTP router, so the honest response to a red build is to stop reading it. `scripts/audit-gate.mjs` walks the lockfile from each workspace's `dependencies` — never its `devDependencies` — through node resolution and fails only on **high or critical advisories inside that set** (551 of 1220 packages). `npm audit --omit=dev` is the obvious answer and does not work here: the two trees are hoisted into one `node_modules` and the lockfile carries no `dev` flag for a workspace's devDependencies, so npm reports the same six advisories with the flag as without it — measured, not assumed. **Exceptions are keyed by GHSA id, which buys two properties**: a NEW advisory against an already-excepted package still fails, and an exception whose advisory has been *fixed* also fails, because a note claiming a risk is being carried when it is not gets read as still true. One is carried: `find-my-way`'s HTTP/2 CONTINUATION DDoS, whose fix is `@nestjs/platform-fastify@11.1.10` — **eighteen patch versions backwards** — and which needs HTTP/2 the `FastifyAdapter` never enables. **ZAP is passive, signed in, and aimed at the dashboard's port**, not the api's: the dashboard answers `/api` itself, so one origin is documents, assets and api at once — production's shape minus the proxy. Active scanning is refused for now on a specific ground rather than caution: it would submit forms as an authenticated owner, and what it would reach is *connect a cluster* and *apply a recommendation*. `fail_action` is on, because a scan that cannot fail is a report nobody is obliged to read. **Signing in took two things, and the second is the one worth recording**: a session (sign up over the api, create an org, hand ZAP the cookie through `ZAP_AUTH_HEADER=Cookie`; `SIGNUP_MODE` left alone because the first account always bootstraps an invite-only install, so the scanned posture is the deployed one) and a BROWSER spider. **The dashboard's pages contain no `href` at all** — measured on the built output, signed in and signed out, zero in both, because TanStack Router navigates on the client — so a link-following spider could never have reached a route however well authenticated. That is what left the first runs scanning 13 URLs, none of them the product; with `-j` the crawl reaches 87, including `/app/settings/account` and `/app/settings/organizations`. The session check asks `/api/org` (200 with the org, 401 without) rather than following `/app`, because `/app` only redirects to a real route once the session resolves an organization — a routing-based check answers "not authenticated" for a state that is. An earlier version grepped `/app`'s body for "sign in" without following the redirect, which returns zero bytes, so grep matched nothing and the check passed for the wrong reason. Signing in added exactly one alert, benign and verified rather than assumed: `sessionStorage` holds TanStack Router's reload guard, keyed `tanstack_router_reload:<error message>`, and no code of ours writes there at all. **Asking what the scan would find turned up the actual defect before it ran**: neither server sent a single security header — no CSP, no `nosniff`, no framing refusal, on either process, and a baseline scan that can fail is unaddable until that is true. Both do now — `nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy`, `no-store` — set in the app rather than at the ingress, because a header that only exists in the chart is one `npm run dev`, the e2e suite and every compose self-hoster do not have, so the one place it is exercised is the one place nobody tests. HSTS is the deliberate exception: from a process reached over plain HTTP inside the cluster it is ignored by every browser, so sending it would be a header that looks like a policy and is not. The api's CSP is `default-src 'none'; frame-ancestors 'none'; base-uri 'none'` — the strongest available and *true*, since it serves no documents. The dashboard's CSP was deferred to #134 and the ZAP rule for it IGNOREd with that number rather than passing quietly; **D54 shipped it, and the `10038` line is gone** | Locked |
| D53 | **The repo's own scripts are TypeScript under `npm run typecheck`, and the audit gate holds both of its parses to a fixture on every run** (#136) | Aug 2026. `scripts/*.mjs` were the only JavaScript left in a TypeScript repo and — the part that mattered — the only source files **no check read**: `npm run typecheck` fans out to the workspaces and `scripts/` belongs to none of them, so a typo in the audit gate was found by CI failing at runtime, or not at all. They are now `.ts` and are run unchanged: Node 26 strips types on the way in, so there is no build step, no `tsx` and no loader flag. The one constraint that comes with that — erasable syntax only, since `enum`, parameter properties and `namespace` parse and *then* fail at runtime — is enforced by `erasableSyntaxOnly` in `tsconfig.scripts.json` rather than remembered. `//#typecheck:scripts` is a turbo **root** task because the per-workspace fan-out has nothing to reach here. The root package gained `"type": "module"`, without which Node reparses every one of these files as ESM and warns `MODULE_TYPELESS_PACKAGE_JSON` on each lint and each version check — a warning is a defect (D20) — and `engines.node` moved to `>=24`, which is now the floor at which `npm run lint` runs at all. **The rename is the cheap half.** `audit-gate` reads two external schemas by hand, the lockfile's `packages` map and `npm audit --json`'s versioned report, and a wrong field name in either does not throw: it yields an empty advisory list and a **green gate that checked nothing**, the worst failure available to a security check. So the reachability walk and the report parse are pure functions, held on every run to a synthetic lockfile carrying one instance of each shape they must understand (a workspace `link`, a `node_modules/a/node_modules/b` shadow, an `optionalDependencies` edge, a cycle, a dev-only subtree that must not be reached) and to a synthetic report; `auditReportVersion` is asserted to be the `2` the parse was written against; and npm's own `metadata.vulnerabilities.total` is cross-checked against the number of entries actually read. Verified from both ends — breaking the upward-resolution loop exits 2, renaming `via.url` to `via.uri` is now a compile error rather than an empty result, and the gate's verdict on the real tree is unchanged. `.mjs` under `.agents/` is out of scope: vendored, and rewriting it forks a file the next skill update replaces | Locked |
| D47 | **The per-cluster reads are capped with an honest count, not paginated — and the cap was chosen from a measurement, not a guess** (#64) | Aug 2026. #18 virtualized the two long tables, which bounded the DOM and nothing else: the payload was still whole. The issue asked for a measurement before a design, so one was taken against a seeded worst case (200 collections × 90 days of hourly readings; 4,000 indexes each with a proposal) on the dev stack: **`getLatencySeries` 30.9 MB / 4.2 s**, `listRecommendations` 1.86 MB / 55 ms, `getCollections` 55 KB / 28 ms, `getLatency` 37 KB. That spread decided all three answers. **The series was the emergency and is not a pagination problem**: it grew on a second axis — every collect, forever — and the dashboard discarded almost all of it, charting four collections out of two hundred. Bounded by a 30-day window (the trend chart is about *recently*; the before/after table still reads the full plan window, which the same test asserts) and a top-N by evidence, with `totalCollections` beside them so the panel can say what it is not drawing. No cursor: nobody wants page nine of a chart. **The tables get a cap with an honest count, not server-side sort/filter.** `listRecommendations` returns the `RECOMMENDATIONS_CAP` (500) highest-scoring rows plus `total`, and the table says "showing the 500 highest-scoring of 4,000". This deliberately keeps D33's client-side sorting and filtering — instant, no round trip, and the empty-versus-filtered distinction survives intact — at the price of not scaling to a reader who genuinely needs row 3,000. Nobody has asked for that; they want the top ones, which is what the default sort already gives. The alternative (filter/sort/cursor params on every read) was priced and declined: it makes typing in the filter box a debounced request, moves D33's default orders into the api as `orderBy`s that need indexes, and is a contract shape #42 would want to be compatible with — a design worth doing when a customer needs it, not before. **`getCollections` is deliberately left unbounded**, and the measurement is why: one row per collection at 55 KB for 500 collections, so it would take ~5,000 collections to reach half a megabyte. The cap is applied in SQL with the default order, because a cap without an order is a random sample | Locked |
| D54 | **The dashboard's documents carry a per-response nonce CSP; `style-src` is the one relaxation and it is written down** (#134) | Aug 2026. D52 shipped every header that needed no tuning and left this one, because it is the only one here that breaks the app silently: TanStack Start emits inline scripts per response — the dehydrated router payload and the buffered `$tsr` block — and a `script-src` without a nonce refuses them, leaving a page that renders and never becomes interactive. That reads as a slow dashboard, not a broken one. **The framework has the seam**: `router.options.ssr.nonce`. `src/server.ts` wraps the stream handler, mints 128 bits per response, hands it to the router and names it in the header built from the same variable — measured on the built output, where **every** script carries it, the module entry and the landing page's JSON-LD block included. `script-src 'self' 'nonce-…'` and **not** `'strict-dynamic'`: that would make browsers ignore `'self'`, and the `<link rel=modulepreload>` tags have no nonce to inherit trust from. With `nosniff` on every response, `'self'` cannot be turned into a script by getting the api to echo JSON at a `<script src>`. `connect-src 'self'` is the whole of it — the issue expected a Sentry ingest host, but the dashboard's Sentry is server-side only and the browser bundle contains none (verified in the built output). **`style-src` carries no `'unsafe-inline'` either**, which took a second pass and was worth it: `'unsafe-inline'` there is ZAP's `10055`, so the first attempt traded one listed alert for another. Every style element written from JavaScript is now allowed by name. `react-remove-scroll-bar` — under every Radix dialog, writing the MEASURED scrollbar width, so unhashable — asks `get-nonce` for a nonce, and `lib/style-nonce.ts` gives it this response's, read off the document's own script tag: browsers hide a nonce from `getAttribute` (measured — it returns `""`) precisely so injected markup cannot reuse one, while the `.nonce` property still answers to script that has already satisfied `script-src`, so nothing is written into the HTML for it. The two that take no nonce are constants of their installed version and are hashed instead: sonner's stylesheet, and `@radix-ui/react-select`'s viewport rule (which does have a `nonce` prop, but reaching it means forking the vendored `components/ui/select.tsx` that the next `shadcn add` would undo). Both hashes are **recomputed from `node_modules` by a unit test**, so an upgrade is a red test naming the constant rather than a toast that silently loses its styling. `'unsafe-inline'` survives only on `style-src-attr`, which React's server-rendered `style={{…}}` requires and which ZAP does not report — verified by scanning both spellings. Three things had to change to reach zero violations, all found by measuring rather than reasoning: the first `style-src-attr` split was wrong (the offenders write style ELEMENTS, not attributes), the last hold-out was Radix Select rather than anything obvious, and **zod probes `Function("")`** to decide whether it may JIT-compile each schema, which the policy refuses — zod's fallback is correct but the browser logs a violation per page load, so `packages/contracts` sets `jitless` in the browser before it builds a schema, which is the only point early enough. **The e2e suite is the gate**: its `test` now carries a fixture that fails any test in which the browser refused anything, so the policy is checked against every page the suite touches rather than a header assertion. **Revised Aug 2026: `style-src` does differ under `vite dev`, and only there.** The strict spelling was measured against the built output and the ZAP job and not re-measured against the dev server, which has no built CSS file — `@vite/client` fetches the stylesheet and applies it by creating a style element, so `npm run dev` rendered the whole dashboard unstyled. Not for want of a nonce on either side: Vite looks one up at `meta[property=csp-nonce]`, and TanStack Start emits that very tag but writes the value to `content` while stamping `nonce` on every other head element it renders. `documentCsp` takes a `dev` flag, set from `import.meta.env.DEV` so a build carries no branch, and the dev spelling **drops** the nonce and hashes rather than appending `'unsafe-inline'` to them — a browser ignores that keyword in any directive carrying a nonce or a hash, which is why the two sit safely side by side elsewhere and why the first attempt changed nothing. Both halves are now asserted by unit tests and were verified against a running dev server and a running build | Revised |
| D55 | **A turbo task with no `dependsOn` is not slower, it is stale: `test` gained `^build`, and the `lint` task that matched no script is gone** (#24) | Aug 2026. First pass of the vendored-skills audit, `monorepo / worker` against `turborepo`. `test: {}` looked like the right shape for a suite that needs nothing built, and it was wrong twice. `@repo/contracts` resolves through `main: dist/index.js`, so `npm test` on a fresh clone could not import it at all — the README's "what it needs: nothing" was aspirational, and is now true. Worse, with no dependency edge turbo has no reason to re-run a suite when the contract it asserts against changes: appending one line to `packages/contracts/src/index.ts` left `@repo/api:test` **FULL TURBO**, and the suite that would have caught the change is the one written to catch it — `contract-routes.test.ts` reads the contract by reflection to enforce that no state-changing route answers a GET, which is the only thing standing between a `SameSite=Lax` cookie and CSRF. Reachable in CI, not only locally: `ci.yml` restores `.turbo` by prefix (`turbo-`), so a PR that touches only the contract gets a warm cache and a skipped check. With `^build` the same probe misses `@repo/api:test` and `@repo/web:test` and still **hits** `@repo/errors:test`, which depends on neither — the edge is the real one, not a blanket invalidation. A transit node would buy the awareness without the wait and is the wrong tool here: these suites need the built output, not merely knowledge that it changed. `lint` was the opposite failure — registered in `turbo.json`, defined in no package, so `turbo run lint` answered "No tasks were executed" while the linting CI depends on ran from the root script outside turbo entirely. Deleted rather than fanned out: biome reads all six workspaces from one config in ~300ms, and per-package passes would cost more than caching them saves. The skill's other compilation strategy, JIT packages exporting `.ts`, stays rejected for an unrelated reason: the api ships as `node dist/main.js` with no bundler in front of it | Locked |
| D56 | **The two audit payloads are typed per act and per kind, because nothing reads them back** (#24) | Aug 2026. Second pass of the skills audit, `api` against `nestjs-expert` and `typescript-advanced-types`, plus the JSONB rule from `drizzle`. Two `jsonb` columns were `Record<string, unknown>`, which is to say untyped, and both are written in exactly one place and read by a person during an incident months later — so `{ readOnly }` and `{ readonly }` were equally acceptable and the second reads back as a mode change with no mode in it. `SecurityEventMetadata` now names the specifics of each of the seven acts that record any, and `SecurityEventInput` distributes over all 23 names so `event` decides what `metadata` may be: the misspelling above is a compile error, and so is attaching metadata to an act that carries none (both verified by planting them). The obstacle was worth the note — **TypeScript will not distribute assignability when the source's discriminant is the whole union**, so `authTrailEntry`'s one shared base object could no longer claim to be a `SecurityEventInput`; the three cases that record specifics now re-state the narrowed `event` beside them, which is exactly the pairing being checked, and `Omit` had to become distributive because a plain one collapses the union back into the any-event-any-metadata type this replaces. `actions.rollback_token` was the same shape of problem and the compiler corrected the premise: the finding said both writers store `{ spec }`, and a third — `jobs/create.ts` on a CREATE row — stores `{ indexName }`. So the column is that union, spelled out, and `rollbackRecommendation` narrows with `in` where it used to re-establish the shape with a Zod schema at runtime. Nothing reads the CREATE variant yet; it is now visible that it exists. Verified with 104 integration tests against a live postgres and mongod, not only by typecheck | Locked |
| D57 | **A route's authorization is an argument of building it, enforced by an oRPC middleware and locked in by a restricted import — not a Nest guard** (#143) | Aug 2026. Authorization was a line inside each handler: 38 call sites, all 24 routes correct, and a route that forgot one would have been public with nothing to say so — not the types, not a test. `route(this.tenancy, contract.x, req, "owner")` is now the only way to build a handler, so the level is an argument rather than a habit, and `biome.json` makes @orpc/nest's own `implement` a restricted import so this stays the only door. **A Nest guard is the wrong tool and the reason is structural**: Nest runs guards BEFORE interceptors, `@orpc/nest` implements a route AS an interceptor, so a guard's `ORPCError` never reaches oRPC's codec — `AppExceptionFilter` finds no `statusCode` on it, falls to its last branch, and renders HTTP 500 with a Sentry report for a working refusal. Every code the dashboard branches on (`TWO_FACTOR_REQUIRED` → account page, `SESSION_NOT_FRESH` → re-auth dialog, `PLAN_LIMIT` → upgrade) would stop arriving. As a middleware they serialize as themselves. The middleware also **hands back what it resolved** — `context.member` and `context.userId`, narrowed by the level, so an owner route reads `context.member.orgId` without re-checking that there is one — which is the half a decorator cannot do, and exactly why better-auth's own Nest package was turned down for this. `TenancyService` memoizes the membership per request the way `auth/session.ts` memoizes the session (#77), because the gate resolves it before the handler and `setClusterMode` still escalates to `requireFreshOwner` inside one; without it that would be two queries where there was one. Four levels and deliberately no `public`: every contract route wants at least a session, the health check is a plain Nest `@Get` outside all of this, and a level for a case that does not exist is a hole nobody asked for. Three things stayed in handlers on purpose — the escalation above (which of owner/fresh applies is a fact about the input, not the route), cluster ownership (three reads answer with an empty shape rather than a refusal, and a stream that ends is not one that never started), and the plan checks, which are entitlement rather than authorization. Held by a test as well as the lint rule, because a rule switched off in a hurry fails silently and the route keeps working for everybody: it reads the controller sources, pairs every `@Implement` with a `route(…, level)` and asserts the contract has no route implemented anywhere else. Verified from both ends — the restricted import fires on a planted `implement`, the test names the offender on a planted un-gated route, and 104 integration tests still pass every refusal | Locked |
| D58 | **A password reset ends every session, and the GitHub token nothing uses is encrypted at rest** (#24) | Aug 2026. Fourth pass of the skills audit, `auth`. Both were better-auth defaults left in place, and both are named in `better-auth-security-best-practices`' own checklist. **`revokeSessionsOnPasswordReset` was off**, which is worse here than in general because this repo already tells people that resetting is the remedy: three of the emails `auth.config.ts` sends say so — "someone has your session or password — reset it now", "someone has your password — change it now", "do NOT click the link — change your password instead". With the option off, all three were advice that did not do what they implied: the intruder's session outlived the reset for the rest of its seven days, and the reader who acted on the warning had locked the door behind the burglar. On, the row is deleted at the reset; the cookie cache still answers for up to its five minutes on a browser holding one, which is shorter than the alternative by seven days. **`account.encryptOAuthTokens` was off**, and better-auth fills `account.access_token` / `refresh_token` on every social sign-in whether anything reads them or not — nothing here does, the token is never used to call GitHub for anybody — so the control plane was storing a live credential to someone else's account, in plaintext, for no feature. Rows written before this stay plaintext and are left alone (better-auth only decrypts what looks encrypted, and there is no reader either way); deleting the columns would be the stronger answer and is the adapter's to make, not ours. The revocation is held by an integration assertion, and getting that assertion right took two corrections worth keeping: a count of surviving sessions is wrong because better-auth mints one on the reset itself, so the claim is "none of the tokens the account held before is left"; and the old cookie's cached copy has to be dropped from the jar before re-trying it, or the request tests the session cache rather than the revocation and would pass with the option off | Locked |
| D59 | **The dashboard honours `prefers-reduced-motion`, and the reason usually given for the spelling is wrong** (#24) | Aug 2026. Fifth and sixth passes of the skills audit, `web` and `ui`. Both areas came back nearly empty, which was the expectation — #11–#18 had already rewritten the web app against the `tanstack-*` guidance, and the checks that matter were already made and already explained: loaders fan out with `Promise.all` over `ensureQueryData` rather than awaiting in sequence, `staleTime` is 30s with the SSR double-fetch written up in `queries/client.ts`, there is one `useEffect` in the whole app and it is in a chart, no component is defined inside another, no numeric `&&` in JSX, `components.json` matches the aliases, no icon-only button lacks a name, no `onClick` on a `div`, and `postgres-best-practices` turns out to be a nine-line stub whose only rule — prefer `text` to `varchar(n)` — the schema already follows with zero `varchar` columns. **The one real gap was motion**: nothing honoured the preference, and there is motion to honour — every dialog and dropdown enters and leaves on `animate-in`/`animate-out`, the skeletons pulse for as long as a read is in flight, the spinner spins, and `scrollRestoration` jumps the page on each navigation. A `@media (prefers-reduced-motion: reduce)` block now shortens all of it. **The part worth recording is the part I got wrong**: the received reason for writing `animation-duration: 0.01ms` instead of `animation: none` is that Radix unmounts a closed dialog on `animationend` and cancelling the animation removes the event, stranding the overlay. That is not true of Radix — `react-presence` checks the computed name first (`else if (currentAnimationName === "none" \|\| styles?.display === "none") send("UNMOUNT")`) — and it was measured, not reasoned: the whole 13-test cluster spec passes under `reducedMotion: "reduce"` with `animation: none`, with `animation-play-state: paused`, and with the duration form. So an end-to-end test written to catch that spelling caught nothing and was **deleted rather than kept as decoration** (D53's rule about a check that passes forever). What remains is a modest unit test asserting the block is present and covers the four properties, which is the failure that was actually available: the rule vanishing in a refactor, unnoticed, because no other test in the repo reads CSS and nobody without the preference set can see the difference | Locked |
| D60 | **Typecheck reads the packages' source, not their `.d.ts` — and it is not the speedup it was proposed as; links preload on intent** (#24) | Aug 2026. Two of the three things #24's audit weighed and left for a decision. **`exports["."].types` now points at `./src/index.ts`** in `@repo/contracts`, `errors` and `metrics`, so `typecheck` drops `^build` for a `transit` node — dependency awareness without the wait, the same shape #142 needed for `test`. **The speedup did not materialise and the claim is withdrawn**: measured 19.3s against 20.0s, which is noise, and `--summarize` says why — `@repo/api#typecheck` alone is 18.3s of an 18.3s run, so the three package builds were never on the critical path, and what they cost the barrier the consumer now pays compiling the same source itself. What the change does buy is that **a stale `dist` can no longer mask an error in source**: with a valid older `.d.ts` in place, a type error planted in `packages/contracts/src/index.ts` now fails `@repo/api`'s typecheck, where before it would have been invisible until the next build. Worth having for its own sake after two stale-artefact misses in one session. **`defaultPreload: "intent"`** on the router runs a link's loader when the pointer settles, which matters most for the cluster page and its seven reads. `defaultPreloadDelay` is raised from the framework's 50ms to **200ms** — 50 is too eager for a rail listing every cluster, where reaching the one you want crosses the ones above it — and `defaultPreloadStaleTime` is stated at 30s so it agrees with the query client's `staleTime` rather than re-running loaders that would answer from cache. Held by an end-to-end test that hovers and counts, written the way the reduced-motion one should have been: it is verified to FAIL without the setting (0 api calls), and it reloads onto another page first, because connecting a cluster leaves the cache warm and a preload would then correctly fetch nothing — green on an empty cache instead of on the behaviour | Locked |
| D61 | **The six `pgEnum`s stay; the sharp edge they come with becomes a lint rule** (#24) | Aug 2026. The last item the skills audit left open. `drizzle`'s guidance is to default away from database enums, and it is house style rather than upstream doctrine, so it was weighed: **the enums earn their keep here** because the columns they guard — `usage_class`, `recommendation_type`, `recommendation_source` — are written by the WORKER from analysis code, not through the oRPC boundary where a Zod schema would catch a bad value. A database that rejects one is a real backstop, and converting six types means a data migration on live rows to buy exactly one thing: cheaper value additions, which happen rarely. The existing split is already the right one — `security_events.event` and `actions.kind` are `text` precisely because new kinds of act arrive often, and that reasoning was written down when they were chosen. **What did not earn its keep was the trap.** Postgres allows `ALTER TYPE … ADD VALUE` inside a transaction and then refuses to let the new value be USED until it commits (`unsafe use of new value … / New enum values must be committed before they can be used`), and drizzle-kit runs one migration file per transaction — so a file that adds a value and backfills with it fails at DEPLOY time, against production, with a message that reads like a typo. Reproduced against this repo's own postgres before writing anything. `scripts/lint-migrations.ts` now fails `npm run lint` on exactly that shape, which turns a failed deploy into a red check at review. Both existing enum-value migrations (0014, 0045) are single-statement already, so this codifies what the repo was doing by instinct. Verified from three ends, per D53: the script passes on the 47 real migrations, flags a planted file that **postgres itself rejects with the same HINT**, and its self-check exits 2 when the `ADD VALUE` matcher is broken — plus it refuses to pass at all if the migrations directory stops matching, which is the other way a check like this passes forever | Locked |
| D62 | **Every KEK version can be named, `MASTER_KEY_V<n>` values are redacted, and the chart can finally deliver a rotation** | Aug 2026. Version 1's key was `MASTER_KEY` and nothing else, which made the FIRST key the one key that could not be retired by name: after rotating to v2 the variable called `MASTER_KEY` had to go on holding the OLD value for as long as any v1 row survived, so the live key was `MASTER_KEY_V2` and the retired one was `MASTER_KEY` — a pair of names that say the opposite of what they hold, read by whoever is mid-rotation. `masterKeyBytesFor` now tries `MASTER_KEY_V<n>` for every n and falls back to `MASTER_KEY` for v1, so nothing existing changes and a retired key can be supplied as `MASTER_KEY_V1`. One consequence is written down rather than guarded: with `MASTER_KEY_VERSION` still 1, a named `MASTER_KEY_V1` also seals NEW rows, because that is what version 1's key now is. **Two things found while reading it, both worse than the thing being fixed.** The boot report redacted secrets from a literal `Set` of NAMES, which dynamic ones can never be in — so a malformed `MASTER_KEY_V2` was reported as `expected 32 bytes of base64 (got "…")`, printing KEK material into the log of the process that refused to start. `isSecretVar()` asks it as a question instead; the test that pins it fails against the old code. And the chart could not deliver a rotation at all: `values.yaml` has told operators to "bump alongside adding `MASTER_KEY_V<n>`" since the rotation shipped, while `secret.yaml` templated `MASTER_KEY` alone and `_helpers.tpl` mounted only that — so the documented instruction was impossible without abandoning the values for `existingSecret`. Both now range over a `secrets.masterKeys` map keyed by version, and over the SAME map, because a key in the Secret that no container reads is a rotation that looks configured and decrypts nothing. Verified by rendering: two keys in the Secret and both mounted into api and worker, nothing rendered when no rotation is configured, `helm lint` clean. Compose passes the same three variables through, blank when unset, which `withoutBlanks` reads as absent. **What this does not do is recover a row whose key is gone** — the DEK is sealed under it, `rotate-key` must `open()` a row before it can re-seal it, and five clusters in the author's local database are past saving for exactly that reason | Locked |
| D63 | **The two dashboard tables take the whole page, and the cell that outgrows its column gets a tooltip rather than a clipped sentence** | Aug 2026. D47 capped the flexible column — 640px on the collections namespace, 620px on the rationale — reasoning that "a table does not have to be as wide as the page it sits on" and that a name far from its own numbers stops reading as one row. Both true, and both outweighed by what the cap cost: the recommendations table stopped at **1508px** however wide the window was. Measured in a browser against the app's own stylesheet, at four container widths: 1200px gave 1198 either way (the container was already the constraint), and 1616 / 2200 / 2880 gave 1508 before against 1614 / 2198 / 2878 after — so between 110px and 1370px of the page went unused on any monitor bigger than a laptop. `flexColumn.max` is now optional and neither table sets one. The box spelling has to move with it and that is the part worth recording: `w-fit max-w-full` made the border hug a table that had a ceiling, and with no ceiling it would measure the same thing twice — shrink-to-fit a full-width table, which under `table-fixed` collapses it to the sum of the stated columns. `w-full` when there is no ceiling, `w-fit` when there is, both asserted. **The rationale column is what makes the cap removable at all.** It was the one cell whose text IS the answer, and `columnWidths` plus the primitive's `whitespace-nowrap` clipped it mid-sentence with no ellipsis to say so and no way to reach the rest. `Truncated` gives it `truncate` and a shadcn Tooltip — but only when the text does not fit, measured as `scrollWidth - clientWidth > 1` and re-measured on resize, because a tooltip on a cell that is fully visible covers the row underneath to say what the reader is already looking at. The pixel of tolerance is load-bearing: sub-pixel layout otherwise reports fractional overhang on text that is plainly not clipped, and nearly every cell would carry one. `title` was rejected for it — unstyleable, invisible to a keyboard, and it stacks the browser's own box on top of ours wherever a cell is already inside a Tooltip. jsdom lays nothing out, so the unit tests stub the two widths and the browser measurement above stands in for what they cannot see | Locked |
| D65 | **A chart tooltip reads the datum off the point, never the pixel — and both of ours were reading the pixel** | Aug 2026. Reported as "what does `1/1 03:00` mean on the graph". It meant the tooltip was formatting a **scene coordinate** as a timestamp: TanStack Charts' `ChartPoint` carries `xValue`/`yValue` (the Date and the µs/op) beside `x`/`y` (where it was painted, in pixels — the library hit-tests with them in `focus.js` and offsets them per facet in `facet.js`), and `formatGroup` was handed `point.x`. A point painted 40px from the left is 40ms after 1970-01-01T00:00Z, which on a Kyiv-time screen prints as `1/1 03:00` — the same heading on every hover, at every zoom, forever. **The value line had it too and was worse**: `format` printed `Math.round(point.y)`, so a read latency of 58,000µs was reported as however many pixels down the line happened to sit — a two- or three-digit number that looks exactly like a plausible latency. The axis was never wrong, which is why this survived: its ticks get a real Date from the scale, so the chart's own labels agreed with reality while the tooltip did not. Both are read off `xValue`/`yValue` now, through two functions that take the POINT rather than a value, so the choice cannot be made again at a call site; the interface names `x`/`y` as present and deliberately unread, which is also what lets a test hand over a point carrying different numbers in each pair. Three smaller things fell out: `timeLabel` returns empty for anything it cannot read rather than rendering it anyway, and it checks `null` FIRST because `new Date(null)` is the epoch — the same wrong answer by another route; and a null point, which is how a gap in the series is drawn, now reads `—` instead of rounding to `0 µs` and reporting an idle collection as an instant one. Held by tests whose points carry a different number in each pair, verified to fail when the pixel is read again | Locked |
| D67 | **The security trail is read owner-only, scoped to the active org, and paged by keyset — and a failed sign-in is drawn without an actor** (#158) | Aug 2026. #53 built the trail and nothing read it: 23 kinds of act, an index built for exactly one query, and no controller making it. Four choices in the reader. **Owner-only**, which departs from the rule everywhere else in this product that a member reads everything in their org — every row carries the address and client a colleague acted from, and who-signed-in-from-where is not team-wide reading. The tab is shown to everyone and the page says so when refused, because a control present for some readers and absent for others is how somebody concludes the feature does not exist. **Scoped to the active org**: `security_events_actor_time` answers "everything this account did" ACROSS orgs, which is an operator's question during an incident and not a tenant's, so no input on the route can ask it. **Paged by keyset**, not capped and not offset: this is the one table that never ages out, so a cap would leave an owner able to see only the newest hundred acts of a trail whose whole value is reaching further back than the day anything was noticed — and the cursor is `(created_at, id)` because accepting an invitation writes rows in the same request and a time-only cursor would skip whichever sorted second, while an offset page would repeat a row that a sign-in pushed across the boundary between loads. **`total` counts what matches the filter**, not the trail. The rule the screen turns on: `SIGN_IN_FAILED` stores the typed address as the TARGET with a null actor because nobody proved they were that person, so the actor column is drawn as an absence and the address is worded as an attempt — rendering it as an actor would accuse the account holder, who in the row worth reading is the victim. `UNPROVEN_ACTOR_EVENTS` is a contract constant so the writer's rule and the screen's rule are one rule. The act NAMES moved to @repo/contracts for the same reason; the per-act metadata shapes stayed in the api beside the call sites, and `event` stays a string rather than an enum because the column is text so that adding an act is a constant and a row under an unknown name still has to render | Locked |
| D68 | **The serving tier folds into one container, and the Services do not move** | **Revised Aug 2026** — two findings from CI, and the first diagnosis was wrong, which is worth recording because the evidence looked conclusive. `chart (single-container)` failed three times in CI and passed every local run: the test read `dashboard ok`, then a refused connection to the api on 3001. **The cause is that a ClusterIP with no programmed backend refuses instantly, and readiness is not that programming** — kube-proxy writes the rule some moments after the pod reports ready. `helm test` starts as soon as the install returns, and in a merged topology ONE pod becoming ready is the LAST thing that happens before it, so the assertion landed inside that gap; timestamps put it 1.2s after readiness. `split` never showed it because its api is ready long before the slower web pod, so the dataplane has converged by the time the test runs. The fix is in the assertion, not the chart: every curl in the chart's own test now retries a refused connection (`--retry-connrefused`), which also makes `helm test` honest for an operator running it by hand, and `kind-test.sh` counts only endpoints whose `conditions.ready` is true — the unfiltered form was reporting a booting pod's address as proof. **The wrong diagnosis, kept:** liveness was `/`, a server-side React render measured at 908ms against kubernetes' one-second default probe timeout, and that looked like the answer. It is a real fragility and it is fixed — liveness is now `/api/health` on the api's own port (1.7ms, no database) in every topology, and every probe carries an explicit `timeoutSeconds: 5`, since the default is too tight for an SSR page anywhere. But it was not this failure: the pod's `Restart Count` was 0 and the api's log showed one clean boot, which the diagnostics added in the same commit are what revealed. Nothing is lost by not probing the dashboard from the merged pod's liveness — the supervisor exits the container the moment either process dies. Original entry follows. Aug 2026. Three Deployments is the right shape for a fleet and more than a self-hosted install needs, so `topology` chooses the packaging while the code stays identical. **One merged shape**, `single-container`: a third image for hosts that run one container per service and have no pods at all (Fly, Render, Cloud Run, `docker run`). Merging containers into a pod merges nothing that is billed — the same two images, the same two runtimes, the same 576Mi of limits — so a shape that stops short of one image saves nothing and buys a shared failure domain. **What makes it cheap is the passthrough** (§14.5): the dashboard already answers `/api` by forwarding it, so one origin is a property of the app rather than of the deployment, and merging the tiers needs no routing change. So the `-api` and `-web` Services keep their names, ports and labels in every topology and simply select the same pod (`component: app`) — the ingress is untouched, and the ServiceMonitors select Services rather than pods, so they are too. Two things do not survive one network namespace and are handled rather than papered over: the **web metrics listener moves to `metrics.port + 1`** (both Services still publish 9464, so nothing that scrapes them has to know), and **port names are pod-unique**, so the dashboard's are `web-http`/`web-metrics` in every topology rather than conditionally — one scheme that always holds beats a name that depends on a value three files away. **Rejected: one process.** Mounting the nitro handler inside Fastify means bridging a WHATWG fetch handler, re-implementing the static layer that serves the built assets and their headers, and streaming SSR through the seam — real risk, for a container count that is already one. So the seam is the process boundary and a supervisor is PID 1: it splits the two variables the processes would fight over (`WEB_METRICS_PORT`, `WEB_SENTRY_DSN` — neither declared by any schema, hence named in `homes.test.ts`), forwards `SIGTERM` to both because `sh -c "a & b"` does not and the api would never drain its pools, and **exits non-zero when either process dies**, so the host restarts the container instead of leaving a dashboard serving 502s from a passthrough with nothing behind it. Two footguns that were prose in `values.yaml` and enforced by nothing became render-time failures, because a merged topology is exactly where an operator meets them: `api.runWorker` with `worker.enabled`, or with more than one replica, installs the crontab twice. Verified per packaging rather than per render — `kind-test.sh` takes `TOPOLOGY`, CI matrixes all three on a packaging change and the two that differ in `worker.enabled` on a job-pipeline one (D74), and the release smoke installs `split` and `single-container` from the registry, since a published image nothing ever started is the failure that workflow exists to catch | Revised |
| D72 | **The idle cost is a cost: the runner polls a fifth as often, its pool is bounded, and the health probe stops writing to the log** | Aug 2026. Three things measured on the built images, all of them paid while nothing was happening. **graphile-worker's own pool was the one left uncapped** by D70's round — separate from our pools is not the same as unbounded, and its default `maxPoolSize` is 10, so up to seven postgres backends per worker were reserved for nothing. Now `WORKER_CONCURRENCY + 2`, derived rather than configured so raising concurrency cannot starve the queue. **`pollInterval` was the 2000 ms default**, which is a query against `_private_jobs` 30 times a minute, 43,200 a day, almost always finding nothing — the tightest schedule in the crontab is five minutes. Raised to 10s after checking WHY it is safe rather than assuming: `add_job` runs `perform pg_notify('jobs:insert', …)` (graphile-worker's own SQL) and the runner holds `LISTEN "jobs:insert"`, so anything enqueued — the dashboard's collect button included — still starts at once, and only work that becomes due by the clock waits. Verified against postgres with `log_statement=all`: the polls land exactly ten seconds apart. **The health route was writing two log lines per request at info**, and a kubelet asks nine times a minute per pod — 18 lines a minute at 211 bytes, about 156 MiB per pod-month of a probe repeating itself, in a repo whose own house rule is that a line in the log should be worth reading. Silenced per ROUTE via an `onRoute` hook setting `logLevel: "silent"`, not globally: every other request line is what the e2e and Kind suites read to tell a working deployment from a quiet one. It is still counted by `instrumentHttp`, so the metric that says whether the api answers does not go quiet with it. Measured after: 18 lines becomes 0, and `/api/clusters` still logs. Rejected in the same pass, having measured it: **gating the metrics SDK when metrics are off** saves under 1 MB — the pipeline holds 4.3 MB with nothing scraping, but 4.8 of that is the import itself, so the earlier claim that "an exporter costs memory in every process" was overstated; what `metrics.enabled=false` actually saves is the listener and the per-scrape control-plane queries | Locked |
| D73 | **The runtime images shipped a third of a build toolchain, and `--omit=dev` does not remove it** | Aug 2026. The api image carried **284 MB of node_modules for 5 MB of application code** — typescript (24 MB), @rolldown (37 MB), drizzle-kit (20 MB) and esbuild (11 MB) among them — behind a Dockerfile comment claiming drizzle-kit was "pruned from the runtime image". It was not. The cause is worth writing down because the obvious fix is a no-op: `turbo prune --docker` regenerates the lockfile for the subset it kept, and the regenerated one carries **no `dev: true` markers**, so npm reads the whole graph as production. Measured three ways — `npm prune --omit=dev` after install (the original), a dedicated `npm install --omit=dev` stage, and reading `.package-lock.json` inside the image — all three keep the toolchain. So the manifests are the lever rather than the flag: `deploy/prod-manifests.mts` deletes `devDependencies` from every manifest in the pruned tree, then the tree is reinstalled, because npm cannot resolve what nothing asks for. api 510 MB → **375 MB**, all-in-one 714 MB → **559 MB**, and what remains at the top is all genuinely runtime (@sentry, @opentelemetry, drizzle-orm, mongodb). It runs AFTER the build and rebuilds node_modules in place, so nothing downstream has to learn a new layout — the alternative, copying a prod tree into a fresh stage, would have meant hand-listing what the runtime needs and the two things that would have been forgotten are `apps/api/drizzle/*.sql` (47 migration files, read from disk by the migrator) and `apps/api/package.json` (read by version.ts). Both are asserted in the verification: migrations apply, the version reads, `@repo/contracts` resolves, the api serves, the worker boots, and both Kind topologies are green | Locked |
| D74 | **CI's chart legs are chosen by what a diff can break under a kubelet** (#177) | Aug 2026. The `scope` job gated `chart` on packaging paths — `deploy/`, `apps/*/Dockerfile`, `ci.yml` — which is the right rule for a PR that moves a React component and the wrong one for #172, whose rewire of `startWorker`, `main.ts` and every job entrypoint matched none of them and skipped the job outright. Nothing else in CI answers that question: the integration suite calls the job functions directly, so `startWorker`, the crontab installing at boot, and a supervisor's view of a process that dies at boot are all invisible to it — the only reason #172 shipped safely is that both Kind topologies were run by hand. So `apps/api/src/jobs/`, `apps/api/src/main.ts` and `apps/api/src/worker.ts` now trigger it. **Both legs, and `worker.enabled` is why**: `split` runs the standalone `worker.ts` Deployment, while `single-container` runs the embedded runner under `api.runWorker=true` and never starts `worker.ts` at all. A job-pipeline change therefore has exactly two kubelet-visible shapes and each topology is one of them. The issue proposed narrowing to `single-container` alone, on the grounds that it is the leg covering the embedded runner, and that would have left the standalone worker with no coverage under a kubelet at all. `chart` is the only gate, the matrix is the literal `[split, single-container]`, and the promtool step is gated on the `split` leg | Locked |
| D75 | **The dashboard loads Sentry on demand; the api does not, and that split is chosen rather than incidental** (#176) | **Revised Aug 2026** — the api's half was reverted after review: a lazy `require` traded away full static typing for the saving, and typing was chosen over it. `initErrorReporting` already returned early with no DSN in both apps, but the cost was the import, not the init — `import * as Sentry from "@sentry/nestjs"` measured 12.5 MB of heap, 322 ms and 715 modules with no DSN, and `@sentry/tanstackstart-react` 17.3 MB and 340 ms. **The dashboard keeps the saving.** `await import()` there is real ESM: fully typed, and ordering is explicitly NOT load-bearing on that side — tracing is off, and `captureException`, the global sink and `wrapFetchWithSentry` need no module patching. Both of its sites are gated (the init in `lib/errors/provider.ts`, and the `wrapFetchWithSentry` in `src/server.ts` that would otherwise have pulled the whole package in regardless of the first). A dynamic import with a literal specifier is still traced by nitro into `.output` — asserted, not assumed. **The api does not, on purpose.** Its ordering IS load-bearing (the SDK patches modules as they are required, which is why `instrument.api.ts` is the first line of `main.ts`), and a real `await import()` cannot substitute there — it is async, so the next `require` in `main.ts` would run before it resolved, which is the exact failure the ordering exists to prevent. What remains is a synchronous `require`, and `require` is declared `(id: string): any` by `@types/node`. A version narrowed with `Pick<>` and checked at load with a runtime guard was built and measured working — cost gone, ordering intact, and the guard proven to fire by deleting each required function from the loaded module in turn. It was reverted anyway: TypeScript cannot verify a dynamically loaded module's shape by any route, and weighed against that residual trust, full static typing won. `errors/reporting.ts` is back to `import * as Sentry from "@sentry/nestjs"`, unconditional, with the SDK's real types and nothing narrowed. `app.module.ts` keeps one thing conditional regardless: the `SentryModule.forRoot()` CALL, not the import — gating which array an already-typed, already-loaded reference lands in costs nothing, so there is no reason to give it up along with the rest. **Verified after reverting, not assumed unchanged.** No DSN: the api now loads all 548 `@sentry/*` modules again (D75's original 0 held only for the reverted mechanism); the dashboard still loads 0. DSN set: identical to before the revert — the SDK still loads first, ahead of pg, mongodb and `@nestjs/core`, all seven integrations present, and an error still reaches a collector with its tags. 628 api and 403 web tests green throughout. Original entry follows. Aug 2026. `initErrorReporting` already returned early with no DSN, which is the default install — but the cost was never the init. `import * as Sentry from "@sentry/nestjs"` was **12.5 MB of heap, 322 ms and 715 modules** measured on the built tree, and `@sentry/tanstackstart-react` **17.3 MB and 340 ms**, paid by every deployment that reports nothing anywhere. Against a 320Mi api limit and a 256Mi web one, that is a meaningful share of both. Three import sites in the api, and two of them were not obvious: `errors/reporting.ts` is the module every other one reaches Sentry through, so `exception.filter.ts` needed no change at all once that was lazy, and `app.module.ts`'s `import { SentryModule } from "@sentry/nestjs/setup"` is itself the load — gating only the `forRoot()` call in a `dsn ? … : []` would have saved nothing. The two apps could not use the same mechanism, and the constraint was the SDK's import-first ordering | Revised |
| D76 | **The images move to an alpine base, and the SRV path was tested on musl rather than reasoned about** (#175) | Aug 2026. After D73 the base WAS the image: `node:26-slim` is 252 MB, `node:26-alpine` is 181 MB, and the api's whole shipped payload is 5 MB of code against a 153 MB tree. Measured after the swap: api 374 → **303 MB**, web 264 → **193 MB**, all-in-one 558 → **487 MB** — 71 MB each, 213 MB across the three, and node_modules identical to the byte on both bases, which is what makes it a base swap and not a rebuild. `find node_modules -name "*.node"` in the shipped api tree returns **0**, so there is no native code to recompile for musl. **The risk was never the packages, it was the resolver**, and this codebase has DNS-shaped history: D64 was `ETIMEDOUT` matched and `ETIMEOUT` not, which quietly sent every `mongodb+srv://` cluster down the wrong path. So the definition of done was a real SRV dial on musl, not a green unit suite. Built for it: a CoreDNS zone serving `_mongodb._tcp.ix.test` SRV plus a TXT record beside it — the Atlas shape — with a mongod behind the names it points at. Results: all three mongo majors' integration suites, **110/110 each on 6.0, 7.0 and 8**, run inside a musl container with `MONGO_URL` set to `mongodb+srv://`, so every collect in every scenario went through an SRV lookup; `kind-test.sh` green on all three topologies with the alpine images; the e2e suite green; and the ZAP baseline **alert-for-alert identical** between the slim and alpine images (FAIL-NEW 0, WARN-NEW 1, IGNORE 4, PASS 62 both), scanned against the images rather than the host build, since the host build is byte-identical across this change and would have proved nothing. **One behavioural difference between the libcs was found, and it is in the reply rather than in the image.** A hostname that does not resolve surfaces as `getaddrinfo EAI_AGAIN <host>` on glibc and as a bare `Server selection timed out after Nms` on musl, because the driver's timeout wins that race; both are already matched by `isUnreachableError`, so the classification is identical, and the note is now next to the regex because that is D64 one libc away. Separately, and worth writing down because it cost an hour: a resolver that answers AAAA with **REFUSED** rather than NOERROR/NODATA makes musl's `getaddrinfo` fail the whole lookup where glibc falls back to the A record. That was a defect in the first test rig — an unconfigured dnsmasq — not in musl or in alpine; `getent hosts` resolved the same name, and against a proper authoritative zone the two libcs agree 5/5 on every lookup mode. It is recorded because a split-horizon corporate resolver could behave the same way, and the symptom would be a cluster that resolves from a slim image and not from an alpine one. Also observed, not fixed here: the all-in-one's node_modules is **338 MB** where the api image's is 153 MB for the same api, because the dashboard's production dependency tree is installed into a runtime that starts the dashboard from `.output` and resolves nothing from it — `@tanstack/react-start` is a dependency of @repo/web and correctly so, but build-time in that image, and D73's lever only removes devDependencies. The two prebuilt lightningcss bindings (gnu and musl, 9.6 MB each, one of which can never load) arrive down that chain. Present on slim too, so this change neither caused nor cured it — fixed separately in D77 (#182) | Locked |
| D77 | **The all-in-one shipped the dashboard's dependency tree, and `dependencies` is where the last 185 MB was hiding** (#182) | Aug 2026. D73 established that the manifests are the lever, and it only ever pulled the `devDependencies` half of it. Measured on the built all-in-one: **338 MB of node_modules where the api image's is 153 MB, for the same api**. The difference was `apps/web`'s *production* tree, installed into a runtime that starts the dashboard from `apps/web/.output` — `node_modules/@tanstack` 27.6 MB, and **two prebuilt lightningcss bindings, `linux-x64-gnu` and `linux-x64-musl`, 9.6 MB each, one of which can never load whatever the base is**. It survived D73 because these are not devDependencies and the manifest is right to list them: `@tanstack/react-start` is the framework, and it reaches lightningcss through `@tanstack/start-plugin-core`, which is `dev: false` in the lockfile so `--omit=dev` keeps it. **The distinction that fixes it is not dev-vs-prod but resolves-vs-does-not**: `apps/web/Dockerfile`'s runtime stage copies `.output` and no node_modules at all, which is the standing proof that the nitro bundle is self-contained, so in *this* image the dashboard's dependencies are build-time. So `prod-manifests.mts` takes `--build-only <name>` and drops that package's `dependencies` too, and a name matching no manifest is an **error** rather than a no-op — otherwise renaming a workspace package silently restores the tree. Measured after: node_modules **338 → 153 MB**, equal to the api image's to the megabyte because every runtime package the two share (`@opentelemetry/api`, `@orpc/*`, `better-auth`, `zod`, the three `@repo/*`) is in `apps/api/package.json` under its own name; image **488 → 322 MB** on the same engine in the same session; `find node_modules -name "*.node"` **2 → 0**, which is what the api image already returned and closes the one place D76's "no native code to recompile for musl" did not hold. Verified beyond the size: `.output` still 15.1 MB, the nitro server booted alone out of the stripped tree and served HTML, the api resolves `@repo/contracts`, all 47 migration files are present, `version.ts` reads its manifest, and `kind-test.sh` is green on all three topologies. The alternative — two builder stages, copying the api tree from one and `.output` from the other — buys the same bytes for a second prune of the three shared `@repo/*` packages and a layout that exists in no other image | Locked |
| D78 | **`version:set` writes the lockfile itself, by editing it rather than by running npm** (#186) | Aug 2026. npm records a workspace's version in `package-lock.json` too, and `set-version.ts` wrote 7 manifests and the chart and not that — so a bump left a tree **`npm ci` refuses** while `version:check`, the one command whose entire job is reporting version drift, printed "agrees". Fixed by hand at 0.3.0, 0.4.0 and 0.5.0, and 0.4.0's own commit message says the script should do it. **The check half matters more than the set half**: `release.yml` runs `version:check` against the tag before it builds or installs anything, so this is what stops a tag being published against a lockfile naming the old version — previously that surfaced later as an `npm ci` mismatch whose message says nothing about releases. Written **directly** rather than by shelling out to `npm install --package-lock-only`, which is what the manual fixes ran: editing cannot re-resolve a transitive dependency as a side effect of renumbering, needs no registry and so no warm cache (a release should not depend on the network to renumber itself), and leaves `set` and `check` reading one list of places so they cannot drift apart. That is only safe because npm's formatting **is** `JSON.stringify(lock, null, 2)` plus a newline — verified byte-identical on the committed 640 KB lockfile — and the result was then verified byte-identical to what `npm install --package-lock-only` produces from the same manifests, so this is the same edit by a cheaper route. Eight fields, not seven: the root's version is stated at the document root *and* as `packages[""]`, while the `node_modules/@repo/*` entries are links with a `resolved` path and no version, which is why a bump has always been a 16-line lockfile diff. An unrecognised `lockfileVersion` or a manifest with no lockfile entry **refuses before anything is written** — found by testing, having first written a version that renumbered the manifests and then bailed on the lockfile, which produces the exact half-bumped tree this entry is about and hides it behind a non-zero exit | Locked |
| D79 | **`trustProxy` is required rather than inferred — `ingress.enabled` is not evidence about what sits in front of the api** (#196) | Aug 2026. The chart derived `TRUST_PROXY` from `ingress.enabled`, and that inference was wrong in both directions. **With an ingress it chose `"true"`** — the one dialect `values.yaml` warns against eight lines above the knob it was setting. Fastify honours it, but better-auth attributes a forwarded header only from RANGES, and with `"true"` it believes `X-Forwarded-For` only when the header carries a single address, which it never does once an ingress has appended itself. So the chart's *default happy path* — `ingress.enabled=true`, `trustProxy` unset — produced exactly the shared auth bucket D41 exists to have fixed, and reported nothing. **Without one it chose `"false"`**, which is only true if nothing is in front. Routing lives outside the chart often enough that this cannot be assumed: a Traefik IngressRoute onto the `-web` Service is a proxy this chart never sees, and every request then carries the proxy's pod address as its socket address — one bucket again, silently, in an install that looks deliberate. **Rejected: a literal `10.0.0.0/8` default.** It is not universal (k3s is 10.42/16, Calico defaults to 192.168/16, an EKS default VPC is 172.31/16), and where it does not match, `TRUST_PROXY` is set, no hop resolves, and the install falls back to the same shared bucket while *looking* configured — a wrong default is worse than none because it stops the operator asking. It also widens trust to the whole pod network rather than to the ingress: this range is who may claim to be someone else, and on a CNI that gives pods VPC addresses that is every workload in the VPC. And a literal default would apply where there is no proxy at all — `docker run`, Compose, Cloud Run — which is what the all-in-one image exists for. The blast radius is cluster-specific, which is the reason the chart must not pick. **So: fail, and report.** `validateTrustProxy` refuses to render when `ingress.enabled` and the value is empty, carrying the candidate ranges in the message so the common case is still one paste — the same shape as the existing `ingress.host` and `databaseUrl` guards, for a failure that is quieter than either. The ingress is only the proxy the chart can *prove*, so NOTES.txt states the effective value at install and says what an empty one means, which is the only cover available for a proxy arranged outside the release. `"true"` stays accepted and is warned about rather than refused: it is valid for Fastify and the chart should not choose it, which is a different thing from forbidding it. `kind-test.sh` moves off `trustProxy=true` to Kind's pod CIDR, because a suite should exercise the dialect the chart tells operators to use. Found in review of the 0.7.0 chart, before any install had an ingress | Locked |
| D80 | **A longer key list is not a wider index: redundancy compares what an index COVERS, not only what it orders** (#204) | Aug 2026. `IndexSpec` had no covering-column concept and the MSSQL collector filtered `ic.key_ordinal > 0`, so `(customer_id) INCLUDE (total, email)` was reported as the bare key list `(customer_id)` — at which point the prefix rule read it as redundant beside `(customer_id, status)` and proposed dropping it. The hide/observe/regression gate would have caught the latency afterwards, which is the wrong place to catch it: the proposal itself is false, and disproving it costs an observe window plus an unhide priced as a full REBUILD (D-shaped asymmetry from #36). Measured on 2022 with 200k rows, `SELECT SUM(total) WHERE customer_id = 42` takes **6 logical reads** through the covering index and **1124** once it is gone with the wider-keyed one still present. So `include` is an optional field on the spec — absent, never empty, for engines that have no such thing, which keeps a persisted mongo spec byte-identical — and `coversIncludes` is a separate predicate rather than a clause inside `isKeyPrefix`, because "are these keys a prefix" and "does that index carry what this one answers" are different questions and only the second one has to hold for a DROP. It guards three places, and the third is the one that would have been missed: the auto-drop rule, the unique-index ADVISORY (whose text claims the data is redundant, which must be true before a human is asked to act on it), and `enforcesTheSame`, where a re-order replacement built without the original's includes enforces the identical constraint and quietly answers fewer queries — invisible to a post-build watch that measures WRITE latency. Includes therefore ride the whole undo path (`serializeSpec` → `parseStoredSpec` → `rebuildOptions`) and the REORDER carry-over in `targetSpec.options`, next to `unique`/`sparse`/`collation` and for the same reason. Two facts came from the server rather than from the docs: `sys.index_columns` orders both halves by `index_column_id` and that order is the DECLARED one (`INCLUDE (total, email)` reports total first though email has the lower `column_id`), and the clustering key is never listed as an extra row — verified for unique, non-unique, filtered and heap indexes — so nothing phantom can satisfy a coverage check. **Not adopted: flagging same-keys-different-includes as redundant.** It is legal on SQL Server, genuinely wasteful, and a NEW finding that should arrive with its own evidence; this change is a guard on an existing one, and equal key lists never reach the prefix rule anyway. MongoDB's executor strips the option rather than forwarding it, since `createIndexes` rejects a specification field it does not know | Locked |
| D71 | **The control-plane database is handed to the job pipeline, not reached for — and the three pools stay three** | Aug 2026. `jobs/db.ts` held a lazy module-level `jobDb()` that fifteen call sites reached into: every job entrypoint opened with `const db = jobDb()`, and `tasks.ts` called it three more times inline. It worked, and it meant the pool's lifetime belonged to whichever module was imported first rather than to whoever composed the worker. **The first instinct was to share the three pools behind a keyed registry, and that was wrong twice.** It would have added a service locator beside two mechanisms that already work — Nest DI on the HTTP side (`DatabaseService` is injected into four consumers) and a hand-rolled deps object on the job side (`ClusterTaskDeps`, which already passes `logger`, `alertOwners` and `emitPassFinished` per invocation and was conspicuously missing `db`) — and it would have destroyed **bulkhead isolation**: one shared pool lets a slow report starve a sign-in of a connection, and three pools of five is fewer connections than one of ten anyway. So the pools stay separate and the WIRING is what changed. `createTaskList(db)` replaces the `taskList` constant, `startWorker(db)` takes it, and each entrypoint takes it as its first argument. `createAuth` stops calling `createDatabase` itself and takes a `db` — the decision that auth gets its own pool moves to `auth/index.ts`, where the composition root can see it. `db` is deliberately NOT added to `ClusterTaskDeps`: nothing reads it there, and that interface's whole value is being three functions wide, so `depsFor(db, helpers)` closes over it instead. Two things fell out that were not the goal. `registerControlPlaneGauges` took a `() => Database` thunk whose only purpose was deferring the singleton, and now takes the database — reading through the api's own pool, so an api serving no jobs stops opening a second pool to answer a scrape. And the embedded runner (`RUN_WORKER=true`) now runs against the api's pool with its stop registered where it is started, which closes a race that existed before: `enableShutdownHooks` registers its SIGTERM handler first, so `DatabaseService` was draining the jobs' pool while a job could still be running against it. `dispatch.test.ts` is the change in miniature — it used to `vi.mock("./db")` to replace the module the function reached for, and now passes a fake as an argument. Verified where it counts: 110 integration tests run the real pipeline against postgres and mongod, and both worker shapes were installed under a kubelet (single-container for the embedded runner, split for the standalone one) | Locked |
| D70 | **The MongoDB driver's pool is bounded, because those sockets are the customer's** | Aug 2026. `mongoClient` passed only a server-selection timeout, so every client took the driver's default `maxPoolSize` of 100. Two things make that the wrong default here rather than merely generous. A session is held PER CLUSTER (`jobs/connection-pool.ts` keeps one refcounted entry each, swept after five idle minutes), so the ceiling multiplies by the size of the fleet rather than being a per-process constant. And the sockets are opened against a database we do not own: spending a hundred of a customer's connection budget to read `$indexStats` is a worse look than any memory it costs us. Ten, from `MONGO_MAX_POOL_SIZE`, which is well above what the collectors ask for — their fan-outs are per replica-set MEMBER and each member has its own client, so the widest concurrent use of one connection is the five reads in `mongo/snapshots.ts`. `maxIdleTimeMS` is set alongside it, at 60s: the pool would otherwise hold whatever the busiest moment needed for the whole five minutes a session is kept warm, and keeping the session is the part worth having, not keeping its sockets. Tested by reading the options back off a constructed client rather than by trusting the call site — a pool bound is easy to write with no effect at all (a misspelt option name leaves the default silently in place), and the same test asserts `minPoolSize` is still 0, so sockets remain a response to demand rather than a reservation, and that the TLS chokepoint still refuses first | Locked |
| D69 | **The memory limit is the knob, and below 512 MiB node stops helping — so the chart caps every heap from its own limit** | Aug 2026. The limits were 256Mi/512Mi per workload because that is what a Node service is assumed to need, and measurement said otherwise. **V8 sizes its heap from the container's limit, not the host's memory**, and grows into that ceiling happily: the api image idles at 228 MB unlimited on a 16 GB machine and 104 MB under 512 MiB, doing the same nothing. So RSS follows the limit rather than the workload, and the instinct is backwards — raising a limit raises usage. Measured serving figures under load (renders, api calls, concurrent sign-ups) are api 99 MB, web 93 MB, worker 65 MB, all-in-one 85 MB, so the defaults came down to 128Mi/320Mi, 96Mi/256Mi, 96Mi/256Mi and 192Mi/384Mi. **The part that was nearly missed**: that sizing only scales down to a point. The default ceiling is 536 MB at 1 GiB and 268 MB at 512 MiB, then FLATTENS — 384 MiB, 320 MiB, 256 MiB, 192 MiB and 96 MiB all give ~262 MB. Which means the first cut of these limits shipped a web container whose heap ceiling (262 MB) was ABOVE its own cgroup limit (256 MiB): a heap-heavy pass would be OOMKilled where it should have been collected, and it was only caught by reading `heap_size_limit` inside the running pod. So the chart now sets `NODE_OPTIONS` per container at 65% of that container's own limit — derived, so changing a limit needs nothing kept in step — and emits nothing below a 64 MB share, where a heap collects instead of serving. `single-container` is deliberately excluded: one cap would be handed to both processes, so its supervisor divides the budget at runtime (40% api, 22% web), and an inherited `NODE_OPTIONS` is its signal to stand aside. The third of the limit left unallocated is not slack: two runtimes' code and stacks live there, and so does the ~32 MB scrypt allocates per password hash IN FLIGHT — outside the heap, bounded by libuv's threadpool rather than by `AUTH_RATE_LIMIT_MAX`, which limits the rate and not the concurrency. That cost is the brute-force resistance and was deliberately not reduced. `WORKER_CONCURRENCY` defaults to 1 for the same accounting (each job holds a cluster's statistics, so it multiplies rather than shares), and `metrics.enabled` now defaults to FALSE: an exporter costs memory in every process, and an install with nothing scraping it was paying that for nobody — `kind-test.sh` and the CI rule check set it on explicitly, so turning the default off did not quietly retire the assertions that cover it | Locked |
| D66 | **Per-node index usage needs no new storage: `index_snapshots.per_member` already IS the history, and a reading that omits a member names it rather than zeroing it** (#161) | Aug 2026. D46 deferred per-node metrics on the grounds that they needed a storage decision on the table D39 names as the growth ceiling. That is true of per-node LATENCY, which has no per-member column and would want one row per member per collect on `latency_samples`. It is not true of per-node USAGE, which is what #161 asks for: `per_member` is written on every collect and is the run's own identity — two collects belong to the same row exactly when it is byte-identical — so the per-node split is already stored, already run-length encoded, and already free. The decision is therefore that there is no storage decision: per-node usage follows the SNAPSHOT history, not the roster, and per-node latency stays deferred with D46's reasoning intact. What was actually missing was a reader — every path summed the array before it reached the screen (`jobs/apply.ts`, `analysis/classify.ts` totalOps), so the whole point of collecting per member was spent on making the total honest. Shape: `listRecommendations` carries a `usage` array BESIDE the rows rather than a field on them, because `recommendation` is also what approve/undo/un-hide return and those answer about a row — a usage field would come back null from every mutation and read as "no recorded usage". The reading is the last collect's batch, keyed by `last_seen_at` for the same reason `getCollections` is. The roster is NOT duplicated into the payload: the page already reads it for the nodes panel, and the members are one fact about the collect rather than 500 copies of one. The rule that makes this safe to ship: a member the collect did not reach is a NAMED blind spot, never a zero and never folded into the "N of M nodes" ratio — a per-node number that silently omits an unreachable secondary is worse than the total it replaced | Locked |
| D64 | **A DNS timeout is a cluster being unreachable, and Node spells that one `ETIMEOUT`** | Aug 2026. `isUnreachableError` matched `ETIMEDOUT` and not `ETIMEOUT`, which are two different errors from two different parts of Node: a socket that gave up is `ETIMEDOUT`, a DNS query that got no answer is `ETIMEOUT`. One letter, and it meant **every `mongodb+srv://` cluster** — which is every Atlas cluster — fell on the wrong side of the classifier, because an SRV string is resolved by `dns.resolveSrv` before a socket is ever opened. Found from a real worker log, where two clusters failed the same tick and only one was handled: the direct-host one logged `unreachable — skipped, retrying on the next tick`, and the SRV one arrived as `querySrv ETIMEOUT _mongodb._tcp.…`, was read as a fault in our code, and cost five attempts at roughly fifty seconds each with a stack trace per attempt. All three callers were affected and all three were wrong in the same direction: the worker burned retries instead of skipping, `AppExceptionFilter` returned 500 instead of 502 **and filed a Sentry report about somebody else's resolver**, and `mapClusterError` gave the dashboard a generic failure where `CLUSTER_UNREACHABLE` is what tells a reader to check the connection string. `ETIMED?OUT` now covers both spellings, and `querySrv`/`queryTxt` join `getaddrinfo` because those three are how a hostname fails to become an address and which one answers depends only on whether the string was SRV. The tests use the messages verbatim from that log and are verified to fail against the old regex. The environment fault that surfaced it was not the app's: rootless podman's aardvark-dns forwards to the host's `/etc/resolv.conf`, which on a systemd-resolved machine is the `127.0.0.53` stub — not reachable from the rootless network namespace, so container names resolved in 1ms from aardvark's own table while every external name timed out. Worth writing down because the app's job is to be quiet about it either way | Locked |
| D46 | **The node roster is one row per cluster, replaced whole every collect — enumeration first, per-node metrics not yet** (#100) | **Revised Aug 2026** — the deferred half has landed for USAGE, and needed no storage decision after all; see D66. Per-node latency stays deferred on the reasoning below. Aug 2026. The dashboard never named a single member: `per_member` was summed before it reached the screen, `MemberConnections` discarded its dial failures by design, and per-node latency existed only at the moment of collection — so partial coverage, an unreachable member and a healthy five-node set rendered identically. This lands the issue's option 1 (the roster) and defers option 2 (per-node latency/usage split), which needs a storage decision on a table D39 already names as the growth ceiling. Shape: `cluster_rosters`, ONE row per cluster, jsonb `nodes` + `collectedAt`, replaced whole on every collect — deliberately not a history table ("which nodes, and did we reach them" is a question about now, and a per-collect log would grow with the cadence for a past nobody asks about), and jsonb because the members are one fact: a half-replaced roster is a topology that never existed. Facts come free: the roster rides the dials the usage pass already makes, plus each answering node's own `hello` (no privilege — it is the pre-auth handshake; `replSetGetStatus` and hidden members stay out of scope, #99). Each node reports its own role rather than trusting the primary's view, because `hello` names only the primary while a node admits to being a secondary itself. `MemberConnections` now records every dial as answered / unreachable / **refused** — refused meaning this deployment's net guard declined the address the cluster named, a policy fact the panel words differently from member health — closing the silent catch where every member failing systematically looked exactly like a standalone. A collect that cannot even establish a roster leaves the previous one standing with its own timestamp: "as of six hours ago" is honest, an empty panel is not. Engine-neutral port (`collectNodes`), so a relational engine is a roster of one | Locked |
| D45 | **A verified account's email change is approved by the old address and proven by the new; an unverified one flips at once — and the signup gate applies to both** (#83) | Aug 2026. `user.changeEmail` was off, so the address an account signs in with — and every notice goes to — could not change at all. On, with better-auth's strongest chain for accounts that have proved their address: the CURRENT inbox gets an approve link (`sendChangeEmailConfirmation` — a stolen session alone cannot move the account), then the NEW inbox gets the verification link (the same `sendVerificationEmail` sign-up uses — a typo cannot receive the account). `updateEmailWithoutVerification: true` for UNVERIFIED accounts, decided rather than defaulted: an address that never proved anything approving its own replacement is theatre, and this is also what keeps the flow usable on installs without SMTP — including the e2e suite, which is why the immediate flow is the one provable end to end. Three edges the issue demanded decided. **The signup gate applies to a change**: `SIGNUP_MODE` decides which addresses may hold an account, and a rename must not be the way around it — refused in a `hooks.before` on `/change-email` with the gate's own reason, before any token is minted, so the reader gets "invite-only" instead of a dead link. **The old inbox is always told**: for verified accounts the approval mail IS the notice; for the immediate flow a "was just requested" mail is sent before the flip (worded as a request, because the flip can still fail — a taken address answers success and changes nothing, better-auth's own anti-enumeration, kept). **In-flight invitations to the old address quietly stop being yours** — `listMyInvites` and accept both key on the session's email; accepted rather than engineered around, they expire in 7 days and re-inviting is one click. Free interlocks: better-auth's `sensitiveSessionMiddleware` demands a sign-in from the last hour on this route (the same hour as #52), and `AUTH_RATE_LIMIT_MAX` already counted `/change-email` among the credential endpoints. The trail records `EMAIL_CHANGE_REQUESTED` at the request, where the actor is — the flip lands on a `/verify-email` GET indistinguishable by path from signup verification | Locked |
| D44 | **2FA is TOTP + backup codes, with an emailed code where SMTP exists, required of owners per deployment posture, and only for accounts a password can open** (#55) | **Revised Aug 2026** — the "no OTP over email" narrowing below was overturned by the founder, and the reasoning on both sides is kept because it is the trade a reader should be able to weigh. **Now**: `otpOptions` is configured, so a reader at the code step can have a six-digit code mailed to the address on the account — five-minute expiry, five attempts, `storeOTP: "hashed"` (a one-time code has no reason to be readable after it is written, unlike the TOTP secret, which must be), on top of the plugin's own 3-per-10s limit for `/two-factor/*`. It is offered **only where the deployment can actually send mail**: `sendMail` no-ops without SMTP, so `hooks.before` refuses `/two-factor/send-otp` with `EMAIL_NOT_CONFIGURED` rather than answering 200 for a code that was never sent — the one outcome worse than not offering it. The button is offered to everyone who reaches the code step, because nothing there knows which factor the account enrolled, and a deployment's own refusal is more use than an absent control. The trail records `TWO_FACTOR_OTP_SENT` alongside the verify pair, since this is the one factor whose delivery leaves the building and a burst of them to one account is somebody working a password they already have. **The argument against, which still stands and is now a known cost**: the mailbox is where `sendResetPassword` lands, so an emailed code is a second factor only against an attacker who has the password and *not* the inbox — against one who has the inbox it is no factor at all, since they could reset the password anyway. And because it is available to every 2FA-enabled account rather than chosen per account, it is also the ceiling: the weakest enabled factor is the one an attacker picks, so an account with TOTP enrolled is worth its email in the limit. Making it a per-account choice instead of a universal fallback is a `user` column and a check on the same hook — the shape to reach for if that ceiling ever matters more than the convenience. Original entry, otherwise unchanged: better-auth's `twoFactor` plugin, with three deliberate narrowings. ~~**No OTP over email**: the mailbox is what a password reset already trusts, so a code mailed there is the first factor in a costume — TOTP and backup codes only.~~ **"Required for owners" is a posture flag** (`REQUIRE_OWNER_2FA`, off by default like `SIGNUP_MODE`): dev, CI and a fresh install bootstrap without an authenticator, a hosted deployment turns it on, and enrolment itself is never gated because it is the way out. Enforcement hangs in exactly two places — `TenancyService.requireOwner` for every api mutation, and a `hooks.before` in auth.config.ts for the four org routes that reshape access (invite, remove, role change, org delete), the latter because organizationHooks are handed the member and never the CALLER (D42's lesson). The org-route check is owner-of-ANY-org on purpose: the posture is per account, not per active org. **Credential accounts only**: an account that arrived through GitHub has no password to phish, cannot enrol a TOTP (better-auth refuses), and brings its provider's second factor — so the gate reads the `account` table live and stands aside. The refusal is its own code, `TWO_FACTOR_REQUIRED`, because the fix (enrol on the account page) is nowhere near the button that was pressed. Recovery is decided, not hand-waved: backup codes are the self-serve path, shown exactly once with the same only-copy treatment as the provisioned connection string; losing device AND codes means the operator flips it off in the database after verifying identity out of band — deliberately no self-serve reset, because any such path is the second factor's bypass. The cookie cache learns `twoFactorEnabled` flips the same way it learns org changes: POST /two-factor/\* expires the cache cookie in hooks.after, or an owner who just enrolled would stay refused for the cache's maxAge — measured by the integration scenario, which plays the authenticator itself (RFC 6238 in ~30 lines) rather than skipping the code path. Interlocks with #52: the re-auth dialog grows a code step and never offers trust-this-device, because freshness exists to prove presence now | Revised |
| D43 | **Going live, rotating credentials and disconnecting need a sign-in from the last hour; the session lifetime itself stays 7d/1d, now on purpose** (#52) | Aug 2026. `requireOwner` asked *are you an owner* and nothing ever asked *are you here right now* — a week-old session on a borrowed laptop answers the first. `TenancyService.requireFreshOwner` layers the second onto exactly three acts, the ones whose blast radius is the customer's database rather than this product: `setClusterMode` to live, `rotateConnection`, `deleteCluster`. Freshness is measured from the session row's `createdAt` — the rolling refresh (`updateAge`) never touches it, so "recently refreshed" cannot impersonate "recently proved" — and the window is `SESSION_FRESH_AGE_SECONDS` (one hour) in `@repo/contracts`, shared because both sides act on it: the api refuses with its own code, `SESSION_NOT_FRESH`, and the dashboard turns that one refusal into a password prompt (`ReauthDialog`) that re-fires the action once the sign-in lands, instead of a toast. Three deliberate edges. **De-escalation is exempt**: flipping BACK to read-only rides plain `requireOwner`, because an emergency stop that waits on a password is not an emergency stop. **Re-auth carries the active org across by hand** — better-auth mints the new session with `activeOrganizationId` null, so without `organization.setActive` a multi-org owner would come back from the prompt silently moved to their oldest org and the retried act would 404 on the cluster they were just looking at. **The lifetime defaults are kept, explicitly**: `expiresIn` 7d / `updateAge` 1d were the library's answer sitting where a product decision should be, and the decision is that with the three dangerous acts gated on freshness, a long session grants reading and benign mutations — shortening it would tax every daily user to shrink a window the fresh tier already closed. better-auth's own `freshAge` is set to the same hour so `deleteUser`-style internal checks agree with ours. The integration test ages a session in postgres and sheds the cookie-cache cookie — the cache would otherwise answer with the young `createdAt` it was signed with for up to five minutes, which is the known revocation lag, not a hole: a real session's `createdAt` never changes | Locked |
| D40 | **Workload analysis runs hourly on its own offset, and urgency is measured in documents walked** | Aug 2026. It used to run behind the collect pass, which meant a missing index waited on a cadence chosen for a different question — and most of the delay was waiting to *notice*, not waiting to act. It now runs hourly on a half-hour offset from collect, so a critical scan goes from first sighting to built index in minutes rather than the better part of a day. Severity is read from `docsExamined` — the documents the server actually walked — rather than from table size, which is the same cost-not-size argument §6.6 makes for which collections earn the pass at all, applied to how urgently one of them needs an index: ≥ 10M walked or ≥ 500k per execution is **critical** and skips the change window under `instantCreate`; ≥ 1M walked, or a collection over 100k documents, is elevated and waits for the window; anything smaller is proposed for a human. Skipping the change window is scoped to creates and to the critical tier only, which is the narrowest reading of D7 that still lets an incident be answered — a drop never skips anything, whatever the urgency | Locked |

### Deferred / open

- **KMS/Vault custodian** — deferred to funded stage / first enterprise deal
  (see §10.3). App-level now.
- **TypeScript 7 (native)** — re-adopt at 7.1 when `tsserver.js` and the
  programmatic compiler API return; also unblocks reverting api to the Nest CLI
  (see D13).

- **`@thallesp/nestjs-better-auth`** — evaluated Aug 2026 (#29), **not adopted**.
  It is what better-auth's own NestJS page recommends, but that page is pointing
  at a community package rather than a first-party integration, and it says the
  library has "beta support for Fastify" — which is the adapter we run. Its
  setup instruction (`NestFactory.create(AppModule, { bodyParser: false })`) is
  Express-shaped and not what the Fastify path does; that path removes and
  re-registers Fastify's content-type parsers instead.

  The reason for the "no" is measured. On Fastify the module serves better-auth
  through middie middleware instead of a Fastify route, and `@fastify/rate-limit`
  only sees routes — so `AUTH_RATE_LIMIT_MAX`, a documented operator knob set in
  `.env.example`, the chart and both test harnesses, **silently stops doing
  anything**. Our own integration test fails, correctly.

  The first measurement said worse than that — 0 of 25 sign-in attempts throttled
  — and it was wrong, because the spike ran without `NODE_ENV=production`.
  better-auth enables its own limiter only in production, and with it set the
  same probe throttles 22 of 25 at better-auth's default 3-per-10s for sensitive
  endpoints. So adopting would not leave auth unprotected: it would *replace* our
  bucket with a tighter one we do not control from the environment. Worth knowing
  on its own account — the production image sets `NODE_ENV=production`, so
  better-auth's limiter is already running alongside Fastify's today.
  The body-parser interaction that looked like the risk turned out not to be
  one: 59 of 60 integration tests passed, so its Fastify JSON parser handles
  oRPC bodies. The prize was smaller than it looked, too — the module replaces
  the 34-line mount in `main.ts`, `auth/http.ts` and `auth/session.ts`, about 57
  lines. `auth.config.ts` is the `betterAuth()` call it takes as *input*, and
  `tenancy.ts`, `signup-gate.ts` and `cookies.ts` are ours. **Revisited Aug 2026
  (#71) and still no**, checked against the package rather than against this
  note. The condition set here — "revisit if we ever adopt better-auth's
  organization plugin" — was met, and the reasons changed while the answer did
  not. **The measured blocker is unchanged**: at 2.7.0 the mount is still
  `httpAdapter.use(...)`, middie middleware rather than a Fastify route, and
  `@fastify/rate-limit` only sees routes — so `AUTH_RATE_LIMIT_MAX` becomes a
  silent no-op and better-auth's non-configurable 3-per-10s replaces it. That
  version predates the spike, so #50 measured this code. It matters more now
  than it did, not less: #54 is open precisely because the auth limit already
  reports a number that is not the one that applies. **And the org decorators
  turn out to be wrong for us rather than merely unhelpful.**
  `@RequireActiveOrg()` refuses a request whose `session.activeOrganizationId`
  is null — which here is not a broken session but the normal one: never
  switched, or switched into an org since deleted, both of which fall through to
  the caller's oldest membership by design. It would 403 every session that
  predates the migration. `@OrgRoles("owner")` inherits that, and adds an api
  call to fetch a role `resolveMembership` already returns beside the org id the
  handler actually needs — which a decorator cannot hand it. What the package
  gets genuinely right is the one thing worth taking: its `AuthGuard` resolves
  the session **once** per request and attaches it, where the api resolved it up
  to three times. Taken without the mount (#77): `requireSession` memoizes the
  resolution per request in `auth/session.ts`, and `session.cookieCache` keeps
  the common case off postgres entirely, re-armed on ordinary responses by the
  `onSend` hook in `main.ts`.
- **TanStack DB** (`@tanstack/react-db` 0.1.95 / `db` 0.6.17 /
  `query-db-collection` 1.2.1) — spiked Aug 2026 (#42), **not adopted**. It fails
  the gate the issue set for it, and fails it harder than expected: a collection
  does not merely start empty on the server, `useLiveQuery` **cannot be
  server-rendered at all**. It calls `useSyncExternalStore` with no
  `getServerSnapshot`, which React requires on the server — no hook in the package
  passes one, and `useLiveSuspenseQuery` delegates to `useLiveQuery`, so it
  inherits the gap.

  Measured on the real app with one collection over `getCollections`, and the cost
  is not scoped to the panel that used it. React's recoverable-error path reverts
  the **whole `/app` shell** to client rendering, so the server HTML went from
  36,660 bytes to 17,127 and `Sign out`, `Collections` and `Policy` all
  disappeared from it. The dashboard still worked — the dehydrated query cache is
  independent of the collection, and the collection reads it on the client with
  **zero** refetches, which is the one part that worked exactly as hoped — but
  every panel was drawn after hydration instead of arriving drawn. The only symptom
  was one line per render in the web server's log: `Error in
  renderToReadableStream: Missing getServerSnapshot`. A warning is a defect (D20).

  Two findings outlive the "no". **The gate named in the issue could not detect
  this** — `renders from what the server sent` runs with JavaScript on, so it
  cannot tell server output from a client re-render off the hydrated cache, and it
  stayed green throughout. A test that reads the raw `/app` response was added
  beside it. And **the module-level collection the docs describe is unusable here
  regardless**: `queryClient` is required config, `createAppQueryClient` is
  per-request on the server, so a module-level collection would serve one tenant's
  rows into the next tenant's render. A `WeakMap` keyed by client is a workaround,
  not a shape the library expects.

  Revisit when the React adapter server-renders, and preferably alongside #22:
  a collection with an SSE sync source updates every live query touching it with
  no invalidation, which is a real argument the read-side rewrite is not. The rest
  of the issue's own case was already thin by its own admission — on-demand sync
  needs filter params no contract in `packages/contracts` takes, differential
  dataflow is irrelevant at a cluster's row counts, and `roi`, `latencySeries` and
  `policy` are not collections. The hand-rolled join it would delete is now six
  lines in `toCollectionRows`, with tests.
- **`nestjs-trpc`** — considered Aug 2026, **staying on `@orpc/nest`**. tRPC infers
  the client's types from the server's implementation, which would replace
  `packages/contracts` — a shared artifact both sides are checked against — with
  a dependency on the api's internals. It would also cost the REST surface: the
  contract carries real routes and generates the OpenAPI document that
  `ingress.api.*` exists to expose, and `/trpc/listClusters?input=…` is not an
  api anyone wants to consume. No pain reported with oRPC, and subscriptions are
  not the deciding factor either — the passthrough already streams (§14.5), so
  #22 does not need a different RPC layer.
- **Agent mode** — phase 2. Interface designed for it from day one.
- **Suggest-mode (`CREATE` from workload)** — higher trust tier; needs profiler
  access. Ship cleanup path first.

---

