// `@total-typescript/ts-reset`. Needed per project, because a tsconfig only sees
// its own `include`; the argument for it is in packages/contracts/src/reset.d.ts
// and is not repeated here.
import "@total-typescript/ts-reset/recommended";
